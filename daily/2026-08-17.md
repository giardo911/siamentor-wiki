# Daily — 2026-08-17

Таймзона: UTC. Cron: daily-siamentor-edtech-unicorn-wiki.

## TL;DR
1. Success: закрыт продуктовый пункт `do_later_recovery_hook`.
2. Day 1 теперь проверен как жесткий activation gate: `plan_created` остается pre-activation, `Сделаю позже` ведет только в recovery, а `book_habit_day1_completed` появляется только после явного completed action.
3. CEO-смысл: SiaMentor не должен считать "получил план" прогрессом. Для D2C book-to-habit платный loop начинается только там, где книга реально стала маленьким действием.

## Что сделано по сайту
- Обновлен eval `scripts/check-access-without-usage-eval.mjs`: проверка теперь привязана к typed `ACTIVATION_CONTRACT`, а не к устаревшим literal event strings.
- Подтвержден UI contract на `/ru/habits`: после выбора книги экран показывает Day 1 action, кнопку `Сделал` и честную альтернативу `Сделаю позже`; recovery surface содержит `do_later_recovery_hook` и CTA `Все-таки сделал`.
- Проверено, что recovery не засчитывается как activation: Playwright asserts events `plan_created`, `do_later_recovery_hook`, отсутствие `book_habit_day1_completed` до completed action, затем activation после `Все-таки сделал`.

## Визуальная проверка
- Desktop screenshot: `/tmp/siamentor-do-later-recovery-desktop.png`.
- Mobile screenshot: `/tmp/siamentor-do-later-recovery-mobile.png`.
- Visual analysis: recovery screen читается как честный "сделать позже" путь, без fake success; основной CTA возвращает к одному маленькому действию. На 390x844 horizontal overflow не обнаружен. Production snapshot pending; проверен свежий local production-like dev surface `http://127.0.0.1:4102/ru/habits`.

## Проверки
- `node scripts/check-access-without-usage-eval.mjs` — pass.
- `pnpm ui:test apps/web/e2e/habit-activation-contract.spec.ts` — pass: 4 passed.
- `NEXT_PUBLIC_API_URL=https://backend.example.com pnpm --filter @siamentor/web build` — pass with existing lint/browserlist warnings.

## Свежие источники

### 1. AI tutors: ценность не в ответах, а в feedback/practice loop
- Источник: https://blog.studywizardry.com/ai-tutors-for-students/
- Дата: 2026-08-09.
- Что значит для SiaMentor: даже в learning framing рынок сдвигается от "AI дает объяснение" к "AI заставляет практиковаться и дает обратную связь". Для book-to-habit это подтверждает жесткий activation gate: план без действия должен уходить в recovery, а не в success.

### 2. RevenueCat: AI apps продают curiosity, но хуже удерживают
- Источник: https://www.revenuecat.com/state-of-subscription-apps/
- Дата: State of Subscription Apps 2026, найдено сегодня.
- Что значит для SiaMentor: AI apps имеют слабую долгосрочную retention относительно non-AI apps, поэтому нельзя оптимизировать только paywall/opening wow. Нужно продавать observable progress: completed actions, streak, verified receipt, return path после "сделаю позже".

### 3. Memory personalization повышает retention, но требует контроля
- Источник: https://mem0.ai/blog/making-ai-companions-truly-personal
- Дата: 2026-07-31.
- Что значит для SiaMentor: companion memory может повышать возвращаемость, если она понятна пользователю. Это поддерживает следующую checklist-задачу: typed `HabitMemoryReceipt` с source, evidence, confidence и user controls.

## Решение дня
Activation truth важнее мягкой конверсии в onboarding. Если пользователь получил план, но не сделал действие, продукт должен сохранить recovery hook и вернуть его к сегодняшнему шагу, а не увеличивать `book_habit_day1_completed`.

## Следующий пункт
Зафиксировать typed `HabitMemoryReceipt`: `source_checkin_id`, `evidence_snippet`, `confidence`, `верно / исправить / забыть`.

## Warnings
- Production deploy/snapshot не выполнялись; prod snapshot pending.
- В `4teacher` до старта были чужие незакоммиченные изменения в тестовой инфраструктуре и Playwright reports; они сохранены и не включались в мой commit.
