# Project checklist

## Execution rules
- [x] Playwright checks are mandatory for UI/e2e changes: run the relevant targeted test plus the full feasible Playwright suite before marking the task done; any failing Playwright test is a blocker, not a warning *(added 2026-07-07 after next_action_uses_verified_memory follow-up)*
- [x] Daily site iteration rule: каждый день брать следующий незакрытый пункт из верхнего CEO checklist, делать его end-to-end, затем смотреть production site, снимать desktop/mobile screenshots, анализировать соответствие acceptance criteria, править визуальные/UX расхождения и доводить экран до polished state перед тем, как закрывать задачу *(added 2026-07-10 after production-oriented SiaMentor backlog reset)*

## 2026-07-10 CEO operating focus: окупаемость
- [x] `book_to_habit_commercial_landing`: сделать `/ru/habits` или `/ru/books-to-habit` первым продающим экраном под оффер "7 дней внедряю книгу в действия"; убрать конфликт с generic AI-learning copy, CTA вести в первый action, а не в "начать обучение" — зачем: текущий `/ru` продает учебный маршрут и бесплатные курсы, поэтому ручной sales traffic попадает в другой продукт; метрика/риск: first-action start rate from sales traffic, offer-message mismatch, bounce before habit flow; acceptance: mobile first viewport содержит offer, 3-step promise, CTA to first action, proof/price teaser, no broad companion claims *(done 2026-07-10: `/ru/habits` refocused on "7 дней действий из книги", navbar CTA switches to `Создать привычку` on habit pages, mobile book cards are compact enough to show the first CTA in the first viewport, and `onboarding_started` carries `acquisition_source=book_to_habit_landing`; commit a06b451)*
- [x] `habit_mobile_cards_density_pass`: уплотнить mobile выбор книги/фокуса, убрать огромные пустые зоны в карточках и довести пользователя до первого действия за 1-2 экрана — зачем: текущая карточка книги почти на экран, что тормозит activation и продажи; метрика/риск: click-to-first-action latency, mobile drop-off before focus lock; acceptance: на 390x844 видны минимум 2 book options или 1 option + clear next CTA, без горизонтального scroll *(done 2026-07-10: mobile 390x844 проверен Playwright, первый экран держит book choice close to CTA без horizontal scroll; covered by `habit-entrypoints.spec.ts`; commits a06b451/b739a8d)*
- [x] `day1_to_day2_next_step`: после `book_habit_day1_completed` показать явный следующий шаг: `Продолжить Day 2`, `Напомнить завтра`, `Открыть следующий шаг`; сохранить receipt и связать его с Day 2 action — зачем: сейчас после 1/3 действий путь обрывается, пользователь не понимает, как дойти до proof paywall; метрика/риск: D1->D2 return, third_action_completed, activation dead end; acceptance: Playwright flow проходит Day 1 completion -> verified receipt -> Day 2 CTA -> next action from receipt *(done 2026-07-10: добавлен stage `day2_next_action_ready`, CTA `Продолжить Day 2`, reminder action, event `d2_return_from_action_receipt`, desktop/mobile screenshots `/tmp/siamentor-day2-next/*-day2-next.png`; commit b739a8d)*
- [x] `proof_receipt_human_copy`: переписать proof modal/receipt с debug-строк `verified: completed_action=...` на человеческое доказательство прогресса: "ты сделал X/Y/Z", "лучше всего сработало Y", "если день плотный Z" — зачем: proof должен повышать willingness-to-pay, а не выглядеть как технический лог; метрика/риск: proof trust, paid CTA click, low-trust paywall; acceptance: в user-facing UI нет raw `verified:` / `source_checkin_id`, evidence cards читаются без технических терминов *(done 2026-07-11: proof paywall принимает human `memoryEvidence`, e2e запрещает raw `verified:`/`source_checkin_id`/`completed_action=`, browser snapshot проверен desktop/mobile; screenshots `/tmp/siamentor-proof-human-copy/desktop-paywall.png` and `/tmp/siamentor-proof-human-copy/mobile-paywall.png`)*
- [x] `mobile_proof_paid_cta_above_fold`: перестроить mobile proof paywall так, чтобы paid CTA и цена были видны без длинного скролла или повторялись sticky внизу — зачем: сейчас на mobile доказательства видны, но главный paid CTA ниже fold; метрика/риск: mobile paid conversion after proof, CTA invisibility; acceptance: на 390x844 видны proof headline, short evidence summary, price/paid CTA и free continue option без потери bounded-memory copy *(done 2026-07-12: mobile proof sheet now uses a compact above-fold offer block with 999 ₽ price, short evidence summary, paid CTA and free continue; Playwright asserts all required elements fit in 390x844; screenshots `/tmp/siamentor-mobile-proof-cta/desktop-proof-paywall.png` and `/tmp/siamentor-mobile-proof-cta/mobile-proof-paywall.png`; production snapshot pending)*
- [x] `paid_offer_event_contract`: заменить demo click `trial_started_from_proof` на revenue-facing contract: `paid_30_day_plan_started`, `price_point`, `acquisition_source`, `sales_outcome`, `free_action_completed_after_paywall` — зачем: dashboard и окупаемость сломаются, если paid event означает просто click/state; метрика/риск: false monetization, attribution blind spot; acceptance: typed events, UI capture, e2e assertion and weekly dashboard can join source/book/price *(done 2026-07-13: proof paywall now captures `paid_30_day_plan_started` with 999 RUB price point, `book_to_habit_landing` source and `sales_outcome`, free continuation carries `free_action_completed_after_paywall=false`, dashboard type tuple uses paid event instead of generic trial, Playwright asserts no `trial_started_from_proof`; screenshots `/tmp/siamentor-paid-event-contract/desktop-proof-paywall.png` and `/tmp/siamentor-paid-event-contract/mobile-proof-paywall.png`; production snapshot pending)*
- [x] Собрать `weekly_profitability_dashboard_v0`: `onboarding_started -> book_habit_day1_completed -> third_action_completed -> habit_paywall_proof_rendered -> free_continue_clicked/trial_started -> paid_30_day_plan_started`, разрезы source/book/price и weekly decision field — зачем: ближайший риск не в UX, а в отсутствии правды о платной воронке; метрика/риск: paid funnel visibility, decision latency, vanity roadmap *(done 2026-07-27: Playwright browser backend recovered; `weekly_profitability_dashboard_v0` targeted e2e passed via `playwright.ui.config.ts`; `/web` build and bounded companion language checks passed; desktop/mobile local production-like screenshots saved at `/tmp/siamentor-weekly-profitability-desktop.png` and `/tmp/siamentor-weekly-profitability-mobile.png`; visual check found source/book/price/weekly decision visible and no horizontal overflow; prod snapshot pending until deploy. Previous partials: 2026-07-14: code/build prepared in `4teacher`, but not marked done because Playwright Chromium executable was missing and browser screenshots were blocked; partial 2026-07-15: build still passes, targeted Playwright still blocked because `chromium_headless_shell-1200` executable is missing and `pnpm exec playwright install chromium` hangs after download, screenshots/prod snapshot still pending; partial 2026-07-16: repeated `pnpm exec playwright install chromium-headless-shell` reached 100% download but left incomplete cache without executable, build passes, targeted Playwright still blocked, screenshots/prod snapshot still pending; partial 2026-07-17: build still passes and `/ru/habits/proof` remains in production build, but `pnpm exec playwright install chromium-headless-shell` again reached 100% then hung before creating executable; no screenshots, prod snapshot pending; partial 2026-07-18: targeted Playwright still fails on missing `chromium_headless_shell-1200`; bounded 120s install again reached 100% then timed out without executable; build passes and `/ru/habits/proof` remains in production build; desktop/mobile screenshots still blocked; partial 2026-07-19: install deleted incomplete cache, downloaded `chromium_headless_shell-1200` to 100%, then hung before creating executable; build passes and `/ru/habits/proof` remains in SSG output; desktop/mobile screenshots and prod snapshot still blocked; partial 2026-07-21: removed broken cache recoverably and retried `pnpm exec playwright install chromium-headless-shell`; download again reached 100% but left only LICENSE/ABOUT, targeted Playwright fails on missing chromium/firefox/webkit executables; `@siamentor/web` build passes, screenshots/prod snapshot still blocked; partial 2026-07-22: `pnpm exec playwright install chromium --with-deps` blocked on root password, plain `pnpm exec playwright install chromium` downloaded Chromium 100% then hung until terminated; `@siamentor/web` build passes and `/ru/habits/proof` remains in SSG output; desktop/mobile screenshots and prod snapshot still blocked; partial 2026-07-25: code contract rechecked in habit-proof-dashboard and habit-proof-paywall e2e, bounded companion language pass, `@siamentor/web` build pass, but chromium-headless-shell install again reached 100% and left no executable; desktop/mobile screenshots and prod snapshot still blocked; partial 2026-07-26: bounded companion language pass and `@siamentor/web` build pass; targeted Playwright for dashboard still fails before browser launch because `chromium_headless_shell-1200` contains only LICENSE/ABOUT and no `chrome-headless-shell`; reinstall again reached 100% then hung, so desktop/mobile screenshots and prod snapshot remain blocked)*
- [ ] Подключить `paid_30_day_plan_price_test`: 499 / 999 / 1490 RUB, где `paid_30_day_plan_started` означает реальный платёж или подтверждённый paid commitment — зачем: окупаемость нельзя проверить click-событием `trial_started_from_proof`; метрика/риск: payment conversion after proof, refund/cancel reason, false monetization
- [ ] Провести `20_manual_sales_interviews`: один канал, оффер "7 дней внедряю книгу в действия", outcome labels `paid / strong_intent / first_action_start / rejected_reason` — зачем: нужен рыночный сигнал willingness-to-pay, а не только product-loop polish; метрика/риск: first-action starts from sales, paid commitments, rejection taxonomy
- [x] Ввести `llm_cost_per_completed_action_guardrail`: считать LLM calls/cost на completed action, activated user и paid user; Day 2-7 template-first, LLM только для shrink/clarify/rewrite fallback — зачем: AI-продукт может конвертить и всё равно не окупаться из-за variable compute burn; метрика/риск: gross margin per paid habit user, cost blind spot *(done 2026-07-28: weekly profitability dashboard now includes `LLM cost / completed action <= 2 RUB; template-first after Day 1`, targeted Playwright/build/language checks pass, desktop/mobile screenshots `/tmp/siamentor-cost-guardrail-desktop.png` and `/tmp/siamentor-cost-guardrail-mobile.png`; 4teacher commit 2d4feef)*
- [x] Заморозить новые companion/recovery guardrails, если они не нужны для dashboard, payment, sales или cost truth — зачем: следующий спринт должен проверять окупаемость, а не накапливать качественные улучшения без денег; метрика/риск: scattered product work, missed July decision gate *(done 2026-07-28: dashboard now exposes CEO guardrail `Freeze new companion/recovery work unless it improves payment, sales, dashboard, or cost truth`, e2e asserts it, desktop/mobile visual check green; 4teacher commit 2d4feef)*

## 2026-07-08
- [x] Реализовать `two_day_reanchor_contract`: после двух missed check-ins показать last verified receipt, один friction-вопрос, действие <=5 минут и переписанный if-then trigger — зачем: повторный пропуск чаще означает плохой habit design, а не слабую мотивацию; метрика/риск: `two_day_reanchor_shown`, `reanchor_action_completed`, D7 retention, notification fatigue *(done 2026-07-30: verified existing current HEAD 6057aab implements `two_day_reanchor_needed` with last verified receipt, friction question, rewritten if-then trigger and `reanchor_action_completed`; `pnpm ui:test apps/web/e2e/habit-two-day-reanchor.spec.ts` passed desktop/mobile; screenshots `/tmp/siamentor-two-day-reanchor-desktop.png` and `/tmp/siamentor-two-day-reanchor-mobile.png`; prod snapshot pending)*
- [x] Добавить copy regression check `bounded_companion_language`: запретить broad/emotional AI companion claims без utility boundary "помню только действия из этой привычки" — зачем: regulatory/privacy фон вокруг AI chatbots и companions повышает цену ambiguous intimacy positioning; метрика/риск: trust complaints, policy/reputation risk, low-trust paywall *(done 2026-08-03: tightened `scripts/check-bounded-companion-language.mjs` to catch broad life-memory/profiling and emotional companion claims in RU/EN, `pnpm test:bounded-companion-language` passed; local browser screenshots `/tmp/siamentor-bounded-companion-desktop.png` and `/tmp/siamentor-bounded-companion-mobile.png`; prod snapshot pending; 4teacher commit 322a467)*
- [ ] Добавить cost guardrail `habit_loop_compute_per_completed_action`: считать LLM calls/cost на completed action и использовать structured fallback для Day 2-7, когда verified memory достаточна — зачем: AI subscription economics зависят от variable compute burn, а SiaMentor должен доказывать habit value дешевым bounded loop; метрика/риск: gross margin per paid habit user, slow check-in latency, open-ended chat cost
- [ ] Обновить post-paywall dashboard cohort: `habit_paywall_proof_rendered -> free_continue_clicked -> free_action_completed_after_paywall -> d7_retained -> trial_started` — зачем: AI apps имеют retention gap, поэтому trial start без продолжения привычки может быть vanity metric; метрика/риск: early-paywall churn, paid curiosity without habit, D7 retained after paywall

## 2026-07-06
- [x] Добавить acceptance test `day2_action_depends_on_day1_receipt`: Day 2 next action обязан использовать Day 1 completed action, friction и trigger, а не генерировать новый generic plan — зачем: свежий AI tutor/agent signal поддерживает curriculum/practice loop, где агент адаптирует следующее задание; метрика/риск: `d2_return_from_action_receipt`, action quality score, generic-plan risk *(done 2026-07-31: Day 2 surface now explicitly includes Day 1 completed action, trigger and friction; added targeted e2e `habit-day2-action-depends-on-day1-receipt.spec.ts`, passed 2/2 desktop/mobile; screenshots `/tmp/siamentor-day2-receipt-dependent-desktop.png` and `/tmp/siamentor-day2-receipt-dependent-mobile.png`; production snapshot pending)*
- [x] Провести copy pass `bounded_habit_companion_only` по onboarding, paywall и memory receipt: заменить broad intimacy/companion claims на "7 дней внедряю идеи книги в действия" — зачем: companion market растет в сторону simulated intimacy, а SiaMentor нужен utility/trust wedge; метрика/риск: trust complaints, low-trust paywall, policy/reputation risk *(done 2026-08-01: Day 2/memory receipt copy now says memory is only for the next book-habit action, removes broad/generic companion wording, targeted Playwright guard checks no broad-memory claims; screenshots `/tmp/siamentor-bounded-companion-day2-desktop.png` and `/tmp/siamentor-bounded-companion-day2-mobile.png`; production snapshot pending; build warning: Next `_document` page data error)*
- [ ] Переставить editable memory receipt перед выдачей следующего действия: `Помню: <trigger/action/friction>` + `верно / исправить / забыть` -> next if-then action — зачем: memory ценна, только если пользователь видит и контролирует ее влияние на практику; метрика/риск: `habit_memory_corrected`, `memory_deleted_by_user`, wrong-next-action complaints
- [ ] Собрать post-paywall continuation dashboard `habit_paywall_proof_rendered -> free_continue_clicked -> free_action_completed_after_paywall -> paywall_dismissed_no_action -> d7_retained -> trial_cancelled` — зачем: AI apps могут продавать curiosity, но проигрывать retention; метрика/риск: misleading paywall CTR, early-paywall churn, paid curiosity without habit
- [ ] Добавить re-anchor rule после двух missed check-ins: last receipt + один friction-вопрос + переписанный trigger + action <=5 минут — зачем: повторный пропуск чаще означает плохой habit design, а не слабую мотивацию; метрика/риск: `two_day_reanchor_shown`, `reanchor_action_completed`, notification fatigue, D7 retention

## 2026-07-03
- [ ] Добавить state `two_day_reanchor_needed`: после двух missed check-ins уменьшить next action до <=5 минут и перепривязать if-then trigger — зачем: свежий habit recovery сигнал трактует повторный пропуск как проблему дизайна привычки, а не мотивации пользователя; метрика/риск: `habit_restart_after_gap_completed`, `two_day_reanchor_needed`, D7 retention, notification fatigue
- [ ] Реализовать `recovery_action_completed` как обязательный outcome для grace/recovery flow: last receipt snippet -> одно действие из книги -> progress restore — зачем: recovery должен возвращать пользователя в цепочку книга -> действие -> привычка, а не просто восстанавливать streak UI; метрика/риск: return-after-gap conversion, streak-break churn, fake recovery
- [ ] Протестировать `proof_paywall_with_free_continue` на третьем completed action: paid 30-day CTA + бесплатный next action + сохраненный receipt/streak state — зачем: AI subscription benchmarks показывают retention gap, поэтому paywall не должен ломать daily loop; метрика/риск: `habit_paywall_proof_rendered`, `free_action_completed_after_paywall`, D7 retained, early-paywall churn
- [ ] Ввести price-test health rule для 499/999/1490 ₽: победитель определяется не по CTR, а по `trial_started`, `free_action_completed_after_paywall`, D7 retained и cancellation — зачем: AI apps могут продавать curiosity без долгого удержания; метрика/риск: misleading paywall CTR, paid curiosity without habit, trial cancellation
- [ ] Добавить eval `intimate_memory_positioning_failed`: fail, если copy обещает broad/emotional memory вместо bounded habit memory — зачем: companion memory повышает privacy/trust risk и может увести SiaMentor из utility wedge; метрика/риск: `memory_outside_habit_domain`, trust complaints, low-trust paywall
- [ ] Собрать July execution chain в одном acceptance test: `book_focus_locked -> book_habit_day1_completed -> d2_return_from_action_receipt -> recovery/free_continue -> D7 retained` — зачем: roadmap Phase 8 должен проверять поведение end-to-end, а не отдельные экраны; метрика/риск: activation inflation, retention blind spot, generic-plan risk

## 2026-07-02
- [ ] Собрать single Phase 8 dashboard chain `plan_created -> book_habit_day1_completed -> d2_return_from_action_receipt -> habit_restart_after_gap_completed/day3_completed -> habit_paywall_proof_rendered -> free_action_completed_after_paywall -> D7 retained` — зачем: RevenueCat retention gap требует видеть один loop от первого действия до paywall continuation, а не отдельные vanity метрики; метрика/риск: fake activation, early-paywall churn, D7 retention
- [ ] Зафиксировать July price-test guardrails для proof paywall 499/999/1490 ₽: тест считается здоровым только если вместе с CTR растут `free_action_completed_after_paywall` и D7 retained — зачем: AI apps могут монетизировать curiosity, но проигрывать retention; метрика/риск: paid curiosity without habit, trial cancellation, misleading paywall CTR
- [ ] Переписать `missed_checkin_recovery_nudge` в calm recovery copy: last receipt snippet + одно действие 5-10 минут + progress restore, без shame/loss framing — зачем: streak design должен возвращать momentum после пропуска, а не превращать разрыв streak в quit moment; метрика/риск: `habit_restart_after_gap_completed`, notification fatigue, streak-break churn
- [ ] Реализовать allowlist и UI-actions для `HabitMemoryReceipt`: `goal`, `trigger`, `completed_action`, `friction`, `source_checkin_id`, `верно / исправить / забыть` — зачем: companion memory ценна только как видимая и контролируемая utility-memory, иначе возникает profiling/trust risk; метрика/риск: `habit_memory_corrected`, `memory_deleted_by_user`, `memory_outside_habit_domain`
- [ ] Добавить eval `paywall_breaks_habit_loop`: fail, если proof paywall после 3 actions не оставляет бесплатный next action или теряет receipt/streak state — зачем: monetization должна следовать за доказанным прогрессом и не ломать daily loop; метрика/риск: `free_continue_clicked`, `free_action_completed_after_paywall`, D7 retention
- [ ] Проверить first-session gate на прибыльный wedge: `plan_created` не может считаться activation, пока нет completed action или явного `do_later_recovery_hook` — зачем: paid habit loop начинается с поведения, а не с красивого 7-day плана; метрика/риск: `book_habit_day1_completed / plan_created`, `minutes_to_first_action_completed`, activation inflation

## 2026-07-01 CEO priority: прибыль до 2026-12-31
- [x] Следующим спринтом закрыть `ProfitabilityPlanH2`: целевой путь к прибыли до 2026-12-31 через 1 платный wedge, 1 acquisition channel и weekly P&L review — зачем: сейчас чеклист много доказывает retention-механику, но CEO-цель требует управлять runway, CAC, conversion и gross margin как одной системой; метрика/риск: monthly net profit, runway weeks, CAC payback, scattered product work *(done 2026-07-01: добавлен `strategy/h2-profitability-plan.md`)*
- [ ] Взять за north-star не `plan_created`, а `paid_habit_users_with_3_completed_actions`: пользователь оплатил после доказанной цепочки книга -> действие -> привычка — зачем: прибыль появится не от AI usage, а от повторяемого paid behavior loop; метрика/риск: paid activation rate, D7 paid retention, fake activation
- [ ] Сделать hard activation gate production-ready: `book_habit_day1_completed` или `do_later_recovery_hook` должны быть обязательными событиями до onboarding success — зачем: без первого действия нельзя честно считать пользователя активированным и нельзя строить paywall proof; метрика/риск: `plan_created_without_action`, `minutes_to_first_action_completed`, D1 return
- [ ] Запустить proof paywall после 3 completed actions с оффером 30-day habit plan и ценовым тестом 499/999/1490 ₽ — зачем: до конца года нужно быстро проверить willingness to pay на visible progress, а не на обещание "AI companion"; метрика/риск: `habit_paywall_proof_rendered`, paywall CTR, trial_started/paid_conversion, refund/trial cancellation
- [ ] Собрать H2 unit economics dashboard: acquisition source -> first action -> 3 actions -> proof paywall -> paid -> D7/D30 retained, плюс AI cost per active paid user — зачем: CEO должен еженедельно видеть, где теряются деньги и где масштабировать; метрика/риск: CAC, LTV proxy, gross margin, cost blind spot
- [ ] Выбрать один GTM-канал на июль и провести 20 ручных продаж/интервью по офферу "7 дней действий из книги с проверяемой памятью" — зачем: до прибыли нельзя распыляться на SEO, community, paid ads и product-led одновременно; метрика/риск: booked calls, first-action starts, paid commitments, slow learning loop
- [ ] Поставить kill/scale критерии на 2026-07-31: scale, если 20%+ новых пользователей дошли до первого действия и 5%+ увидевших proof paywall оплатили; kill/pivot copy/channel, если нет — зачем: CEO-решение должно резать слабые гипотезы быстро, чтобы успеть к 2026-12-31; метрика/риск: decision latency, vanity roadmap, missed profitability window

## 2026-06-27
- [ ] Добавить Phase 8 metric `minutes_to_first_action_completed` и alert на `plan_created_without_action > 35%` — зачем: Stanford/SCALE показывает, что access к AI tutor не равен meaningful usage; метрика/риск: activation truth, motivation gap, false-positive onboarding
- [x] Ввести eval `access_without_usage`: fail, если пользователь получил план/совет, но flow не довел до `book_habit_day1_completed` или `do_later_recovery_hook` — зачем: SiaMentor должен закрывать переход книга -> действие, а не просто выдавать AI output; метрика/риск: `book_habit_day1_completed`, D1 return, answer-machine risk *(done 2026-07-29: added golden eval cases `access_without_usage`/`valid_day1_activation_path` plus `scripts/check-access-without-usage-eval.mjs`; validator, bounded companion language, and web build passed; desktop/mobile local browser screenshots `/tmp/siamentor-access-without-usage-desktop.png` and `/tmp/siamentor-access-without-usage-mobile.png`; production snapshot pending)*
- [x] Спроектировать Day 2 return surface от receipt: "вчера ты сделал X в trigger Y" -> memory receipt -> одно действие 5-10 минут — зачем: повторный вход должен опираться на доказанное действие, а не на абстрактный 7-day plan; метрика/риск: `d1_return_from_action_receipt`, `daily_habit_checkins_completed`, wrong-next-action complaints *(done 2026-08-02: Day 2 surface now shows explicit memory receipt with completed action + trigger + friction/confidence, next action header is constrained to one 5-10 minute action, `habit_memory_receipt_verified` carries progress fields, targeted Playwright desktop/mobile passed and saved screenshots `/tmp/siamentor-day2-return-surface-chromium-desktop.png` and `/tmp/siamentor-day2-return-surface-chromium-mobile.png`; web build compiled/lint/types but still fails on existing Next `/_document` page data error; production snapshot pending)*
- [ ] Добавить dashboard chain `plan_created -> day1_completed -> d1_return_from_action_receipt -> day3_active_checkin_rate -> proof_paywall_rendered -> free_continue_clicked/trial_started` — зачем: AI subscription retention gap требует видеть, где ломается habit loop до и после paywall; метрика/риск: retention blind spot, early-paywall churn, misleading conversion
- [ ] Протестировать post-paywall continuation: бесплатный next action после proof paywall должен сохранять receipt, streak/grace state и путь к later paid conversion — зачем: paywall после 3 actions должен монетизировать proof, но не превращаться в quit moment; метрика/риск: `free_action_completed_after_paywall`, `trial_started_after_proof`, D7 retention *(partial 2026-07-20: локально добавлен continuation state с receipt/streak/grace и later paid CTA, targeted e2e расширен, `@siamentor/web` build pass; не закрыто, потому что Playwright Chromium executable отсутствует, desktop/mobile screenshots pending; 4teacher changes left uncommitted)*
- [ ] Провести copy pass `utility_first_habit_companion`: убрать формулировки broad emotional companion и "глубоко знаю тебя", оставить "память помогает следующему действию из книги" — зачем: companion/privacy risk делает broad memory framing уязвимым; метрика/риск: trust complaints, `memory_outside_habit_domain`, low-trust paywall *(partial 2026-07-23: source copy tightened in `habit-book-selection.tsx`, build pass, commit `06f02e3`; не закрыто, потому что Playwright Chromium executable отсутствует и desktop/mobile screenshots pending; partial 2026-07-24: `test:bounded-companion-language` pass, `@siamentor/web` build pass, production-like local server ready on `127.0.0.1:3017`, retry `playwright install chromium` downloaded 100% but executable still absent, screenshots/prod snapshot still pending)*

## 2026-06-26
- [ ] Зафиксировать `ActivationContract`: `plan_created` = pre-activation, `book_habit_day1_completed` = activation, `do_later_recovery_hook` = допустимое незавершение — зачем: свежий Atlantic/Chalkbeat сигнал показывает motivation gap у AI tutors; метрика/риск: plan-created-without-action drop-off, D1 return, false activation
- [ ] Протестировать Day 1 hard path `book insight -> if-then trigger -> micro-action 5-10 min -> receipt` без open-ended chat exit — зачем: SiaMentor должен переводить книгу в действие до того, как пользователь уйдет; метрика/риск: `book_habit_day1_completed`, action quality score, answer-machine risk
- [ ] Реализовать allowlist для `HabitMemoryReceipt`: `source_checkin_id`, `evidence_snippet`, `confidence`, `goal`, `trigger`, `completed_action`, `friction`, correction actions — зачем: EDPS companion signal требует контролируемой personalization; метрика/риск: `memory_outside_habit_domain`, `habit_memory_corrected`, trust complaints
- [ ] Добавить proof paywall метрики `free_continue_clicked`, `next_action_completed_after_free_continue`, `trial_started_after_proof` — зачем: RevenueCat AI retention gap означает, что paywall не должен ломать только что возникший habit loop; метрика/риск: early-paywall churn, trial cancellation, D7 retention
- [ ] Разделить dashboard cohorts `plan_created_without_action` vs `book_habit_day1_completed` и сравнить D1/D7 retention — зачем: first-session action важнее общего onboarding completion; метрика/риск: activation truth, retention blind spot, misleading DAU
- [ ] Добавить eval `motivation_gap_unhandled`: fail, если ответ дает длинный план/совет, но не приводит к одному сегодняшнему действию с trigger и check-in question — зачем: AI companion должен закрывать переход к практике, а не только объяснять книгу; метрика/риск: unsupported advice rate, `daily_habit_checkins_completed`, D3 active check-in rate

## 2026-06-25
- [ ] Добавить dashboard slice `plan_created -> book_habit_day1_completed -> d1_return -> day3_completed -> same_trigger_completed_days/active_days` — зачем: свежие AI tutor usage-сигналы показывают, что доступ и минуты не равны прогрессу; метрика/риск: activation truth, false-positive engagement, D7 retention
- [ ] Закрыть Day 1 без completed action только через явный `do_later_recovery_hook` — зачем: first session должна доказать переход книга -> действие, иначе продукт остается планировщиком/answer machine; метрика/риск: `book_habit_day1_completed`, plan-created-without-action drop-off, D1 return
- [ ] Зафиксировать typed `HabitMemoryReceipt`: `source_checkin_id`, `evidence_snippet`, `confidence`, `верно / исправить / забыть` — зачем: EDPS companion/privacy сигнал требует контролируемой personalization вместо скрытого профилирования; метрика/риск: `habit_memory_corrected`, `memory_outside_habit_domain`, wrong-next-action complaints
- [ ] Зафиксировать typed `ProofPaywallReceipt`: `completed_actions[3]`, cautious `best_trigger_pattern`, `friction_adjustment`, `next_30_day_promise`, `free_continue_available` — зачем: RevenueCat показывает AI retention gap, поэтому paywall должен продавать observable progress и не ломать habit loop; метрика/риск: `habit_paywall_proof_rendered`, `free_continue_clicked`, trial cancellation
- [ ] Провести copy safety pass: убрать обещания emotional attachment, deep personality knowledge и broad life profiling из onboarding/paywall — зачем: Guardian backlash показывает, что companion framing быстро становится trust/reputation риском; метрика/риск: trust complaints, low-trust paywall, policy/reputation risk
- [ ] Добавить eval `answer_machine_without_practice` в golden set Day 1-3 — зачем: Stanford/Chalkbeat/The 74 сигнал показывает, что unguided AI tutor не удерживает usage; метрика/риск: action quality score, unsupported advice rate, D3/D7 retention

## 2026-06-24
- [ ] Добавить copy/eval boundary `utility_companion_not_emotional_attachment` для onboarding, Day 2-7 и paywall — зачем: свежий Guardian сигнал показывает backlash вокруг AI learning tools из-за cognitive offloading и emotional attachment; метрика/риск: trust complaints, low-trust paywall, policy/reputation risk
- [ ] Зафиксировать first-session hard path `book anchor -> if-then trigger -> micro-action -> receipt`, где `plan_created` не завершает activation — зачем: Stanford/Chalkbeat сигнал показывает, что доступ к AI tutor без guided usage быстро превращается в неиспользуемый продукт; метрика/риск: `book_habit_day1_completed`, plan-created-without-action drop-off, D1 return
- [ ] Добавить eval `answer_machine_without_practice`: fail, если ответ пересказывает книгу или советует без trigger, действия до 10 минут и check-in question — зачем: SiaMentor должен превращать книгу в действие, а не становиться Q&A по self-development; метрика/риск: action quality score, unsupported advice rate, D7 retention
- [ ] Реализовать `HabitMemoryReceipt` как обязательный gate перед Day 2-7 action: `source_checkin_id`, `evidence_snippet`, `confidence`, `верно / исправить / забыть` — зачем: AI companion/memory category требует контролируемой personalization, а не скрытого профилирования; метрика/риск: `habit_memory_corrected`, wrong-next-action complaints, `memory_outside_habit_domain`
- [ ] Проверить proof paywall с бесплатным продолжением: после 3 completed actions показать paid 30-day CTA и бесплатный next action без потери progress — зачем: RevenueCat показывает AI monetization premium при retention gap, поэтому paywall должен доказывать ценность и не ломать habit loop; метрика/риск: `habit_paywall_proof_rendered`, `free_continue_clicked`, trial cancellation, early-paywall churn
- [ ] Добавить trust/retention dashboard slice `plan_created -> day1_completed -> d1_return -> day3_completed -> proof_paywall_rendered -> free_continue_clicked/trial_started`, плюс `habit_memory_corrected` и `wrong_next_action_reported` — зачем: риски companion trust и paywall pressure не видны в DAU; метрика/риск: retention blind spot, trust metric blind spot, hidden churn driver

## 2026-06-23
- [ ] Протестировать state `proof_paywall_with_free_continue`: после 3 completed actions показать proof paywall, paid CTA и бесплатный сегодняшний шаг без потери progress — зачем: monetization должна следовать за доказанной цепочкой книга -> действие -> привычка, но не ломать только что возникший ритм; метрика/риск: `habit_paywall_proof_rendered`, `free_continue_clicked`, `today_action_completed_after_paywall`, early-paywall churn
- [ ] Зафиксировать `ActivationContract` для Phase 8: `plan_created` не считается успехом без `book_habit_day1_completed` или `do_later_recovery_hook` — зачем: свежие AI tutor исследования показывают, что доступ без использования не дает outcomes; метрика/риск: activation rate, plan-created-without-action drop-off, D1 return
- [ ] Добавить eval `action_too_large_or_unbound`: fail, если next action больше 10 минут или без конкретного if-then trigger — зачем: adult D2C habit loop должен конкурировать с рабочим днем через маленькое contextual action; метрика/риск: first-action completion, action quality score, D3 active check-in rate
- [ ] Реализовать `HabitMemoryReceipt` перед Day 2-7 action: `source_checkin_id`, `evidence_snippet`, `confidence`, `верно / исправить / забыть` — зачем: persistent memory становится expectation, но trust требует видимой и исправляемой памяти; метрика/риск: `habit_memory_corrected`, wrong-next-action complaints, `memory_outside_habit_domain`
- [ ] Описать typed contract `HabitProofPaywall`: `completed_actions[3]`, `best_trigger_pattern`, `friction_adjustment`, `next_30_day_promise`, `free_continue_available` — зачем: AI apps имеют monetization premium, но retention gap; paywall должен продавать доказанную continuity; метрика/риск: `habit_paywall_proof_rendered`, paywall CTR, trial cancellation
- [ ] Добавить dashboard funnel `plan_created -> book_habit_day1_completed -> d1_return -> day3_completed -> proof_paywall_rendered -> free_continue_clicked/trial_started` — зачем: usage friction и paywall pressure видны только в цепочке событий, а не в DAU; метрика/риск: retention blind spot, early-paywall churn, trust metric blind spot

## 2026-06-22
- [ ] Добавить `d1_return_from_action_receipt` и `d3_active_checkin_rate` в Phase 8 dashboard — зачем: свежий AI tutoring сигнал показывает, что главная проблема не "умеет ли tutor", а возвращается ли пользователь к практике; метрика/риск: D1/D3 retention, plan-created-without-action drop-off
- [ ] Закрыть first-session success без Day 1 completion: onboarding должен завершаться только через `book_habit_day1_completed` или явный `do_later_recovery_hook` — зачем: book-to-habit должен сразу превращать книгу в действие, иначе становится answer machine; метрика/риск: activation rate, D1 return, generic-chat risk
- [ ] Спроектировать Day 2 action-first surface: receipt прошлого действия -> memory receipt -> один if-then trigger -> микро-действие до 10 минут -> check-in — зачем: AI companion value реализуется через повторяемую guided practice, а не через новый совет каждый день; метрика/риск: `daily_habit_checkins_completed`, same-trigger completion, D7 retention
- [ ] Добавить copy/eval boundary `memory_helps_next_action_only` — зачем: рынок personal AI memory растет, но SiaMentor должен обещать только bounded habit memory, а не широкое профилирование личности; метрика/риск: `memory_outside_habit_domain`, trust complaints, overgeneralized_memory failures
- [ ] Проверить proof paywall на связку `completed_actions[3] + cautious_trigger_pattern + friction_adjustment + free_continue_available` — зачем: AI apps имеют WTP, но retention gap; paywall должен продавать observable behavior proof без давления; метрика/риск: `habit_paywall_proof_rendered`, paywall CTR, early-paywall churn
- [ ] Добавить dashboard slice `action_receipt_seen -> next_action_started -> checkin_completed -> proof_paywall_rendered -> free_continue_clicked/trial_started` — зачем: retention и trust ломаются между receipt, действием и monetization, а не только на DAU; метрика/риск: funnel blind spot, trust metric blind spot


## 2026-04-28
- [x] Определить ICP на ближайшие 30 дней (1 сегмент) и сформулировать оффер в 1 предложение *(done 2026-04-29: primary ICP на 14 дней + оффер)*
- [x] Запланировать Phase 4 (Image Generation Pipeline): выписать 5 планов 04-01..04-05 и оценить время *(done 2026-05-03)*
- [x] Добавить guardrails для LLM/агентов: лимит стоимости/итераций + валидация структурированных ответов *(done 2026-05-03)*
- [x] Спроектировать programmatic SEO шаблон: summary-first + FAQ + таблица + CTA (30–50 страниц пилот) *(done 2026-05-04)*

## 2026-04-29
- [x] Выбрать 1 primary student ICP на 14 дней (экзамен/дедлайн/новая профессия) и сформулировать оффер в 1 строку (student-first) *(done 2026-04-29)*
- [x] Определить activation event: что именно считается первой meaningful learning session (логика + событие + метрика) *(done 2026-04-29)*
- [x] Сформулировать «dosage + progress promise» (D2C аналог outcomes-based): сколько сессий/нед и какой измеримый прогресс обещаем/компенсируем *(done 2026-05-10)*
- [ ] Набросать прототип сценария «tutor + 2 peers» для 1 темы (дешевый social learning эффект) и решить, что меряем в A/B
- [ ] Прописать guardrails по unit economics: лимит шагов/токенов/контекста + smart routing моделей по сложности шага

## 2026-04-30
- [ ] Встроить в определение meaningful session Socratic/scaffolded паттерн: студент сначала отвечает/рассуждает, AI не дает готовый ответ сразу
- [ ] Добавить step-level feedback в сценарий первой сессии: после ответа студент получает конкретный следующий шаг, а не просто «верно/неверно»
- [ ] Сформулировать durable-skill guardrail: минимум 1 вопрос на рефлексию «почему это верно?» или «как бы ты решил похожую задачу?»
- [ ] Сформировать простой тарифный черновик: базовая подписка + лимит N meaningful sessions/нед + понятный апгрейд
- [ ] Добавить cost guardrails для AI-сессий: caps по шагам/токенам и routing дешевой/дорогой модели по типу учебного шага

## 2026-05-01
- [ ] Позиционирование (экзамен-ICP): написать 10 личных сообщений в 2–3 сообщества (Telegram/ВК) с оффером «план на каждый день с проверкой» и ссылкой на продукт; собрать 3 ответа/отказа и выписать причины
- [ ] Retention loop: добавить Exam Streak (счетчик дней подряд) + вечерний reminder только если за день не было `meaningful_learning_session_completed`
- [ ] Монетизация (WTP): зафиксировать paywall на 5-й meaningful session и протестировать цену 299 vs 499 ₽ (минимальный copy + кнопка “оплатить”/“позже”)
- [ ] Learning data moat: определить минимальные 3 поля semantic learning memory, которые обновляются после каждой meaningful session (weakest_topic, misconception_tag, next_topic)
- [ ] Метрики: завести простой срез D0-D1 Activation Rate и D1/D7 retention по exam_date (есть/нет) и subject

## 2026-05-06
- [x] Обновить `strategy/annual-product-strategy.md` под новый wedge: **D2C book-to-habit AI companion** (заменить student/exam как primary), пересобрать NSM/activation/retention под 14-дневный MVP *(done 2026-05-06)*
- [x] Выбрать 3 стартовые книги для MVP (Atomic Habits / 7 навыков / 4-часовая рабочая неделя) и сделать быстрый старт без upload (книга→цель→Day 1) *(done 2026-05-06)*
- [ ] Зафиксировать activation event для wedge: `book_habit_day1_completed` и вывести в аналитике D0 start→Day1 completion
- [ ] Переписать задания Day 1–3 в формате implementation intentions (if-then) + выбор триггера пользователем
- [ ] Добавить “grace day” (1 пропуск без обнуления streak) для снижения отвалов после первого пропуска
- [ ] Streak: добавить micro-reward (минимальная анимация/бейдж) на продление серии, особенно Day 1–7
- [ ] Тест монетизации: paywall/lead-capture после 3 выполненных заданий (варианты 299 ₽ за 30-дневный план vs 699 ₽ за месяц доступа)

## 2026-05-07
- [ ] Обновить daily: собрать инсайты по streak/grace day и if-then механикам + связать с метриками MVP

## 2026-05-08
- [ ] Day 1–3: добавить if-then + выбор триггера + (A/B) 30-сек mental rehearsal перед подтверждением выполнения
- [ ] Paywall/lead-capture после 3 выполненных заданий: сравнить 299 ₽ за 30-дневный план vs 699 ₽/мес доступа (минимальный копирайт)
- [ ] Landing/шаринг: сделать публичный артефакт "7-дневный план по Atomic Habits" (без upload) для ручного набора 50–100 пользователей

## 2026-05-09
- [ ] Day 1–3 контент (Atomic Habits): выписать 3 задания в формате if-then (≤10 минут) + 3 варианта триггера (время/место/событие) как baseline для генерации
- [ ] Онбординг: добавить выбор триггера с дефолтом (чтобы не увеличивать drop-off), сохранить как часть плана
- [ ] Retention после Day 7 (черновик): концепт «Day 8: закрепление внедрения» (1 экран) — inspired by Readwise Mastery daily ritual

## 2026-05-31
- [ ] Синхронизировать `/data/workspace/4teacher/.planning/PROJECT.md` и `.planning/STATE.md` с Phase 8 Habit Tracker из `.planning/ROADMAP.md` — зачем: убрать конфликт между lesson-illustrations фокусом и book-to-habit MVP; метрика/риск: снижает solo-founder execution risk и риск строить не тот wedge
- [ ] Создать golden set для генерации Day 1–3 habit actions: 3 книги x 3 дня x 2 пользовательские цели + expected outputs в if-then формате — зачем: зафиксировать качество микро-действий до изменения модели/промптов; метрика/риск: `book_habit_day1_completed`, task_completion_rate, model drift risk
- [ ] Добавить минимальный eval-скрипт/ручной чеклист для habit action generation rubric: ≤10 минут, один trigger, связь с книгой, конкретный check-in question — зачем: не выпускать generic self-help задания под видом персонализации; метрика/риск: activation rate, D1 return, trust risk
- [ ] Спроектировать typed habit memory schema для Phase 8 (`goal`, `trigger`, `completed_action`, `friction_tag`, `confidence`) до реализации LLM-персонализации — зачем: memory должна усиливать следующий action, а не накапливать шумный чат; метрика/риск: D7 retention, memory pollution risk, personalization quality
- [ ] Добавить PostHog events для Phase 8: `habit_action_generated`, `habit_memory_written`, `book_habit_day1_completed`, `daily_habit_checkins_completed`, `habit_paywall_viewed`, `habit_paywall_clicked` — зачем: проверить loop фактами, а не ощущениями; метрика/риск: activation, NSM, paywall CTR, analytics blind spot
- [ ] Подготовить paywall artifact после 3 completed actions: экран “ты внедрил 3 действия из книги” + оффер 30-дневного плана — зачем: монетизация должна идти после value proof; метрика/риск: paywall CTR, cancellation risk, WTP validation

## 2026-06-17
- [ ] Добавить soft boundary в daily check-in: после выполненного action показывать summary, next if-then step и закрывающий CTA вместо продолжения open-ended chat — зачем: свежий Siri wellness signal показывает риск чрезмерной conversational зависимости; метрика/риск: `daily_habit_checkins_completed`, session length, generic-chat risk
- [ ] Ввести `ai_cost_per_completed_checkin` и budget для Phase 8: reuse 7-day plan, один structured generation call на daily check-in, fallback без LLM для простых states — зачем: RevenueCat напоминает, что AI engagement несет переменную стоимость и может съедать subscription margin; метрика/риск: gross margin, latency, cost per retained user
- [ ] Добавить eval/event `memory_outside_habit_domain` для любых memory writes вне allowlist `goal/trigger/completed_action/friction/confidence/source_checkin_id` — зачем: companion risk у EDPS завязан на personalization + memory, поэтому SiaMentor должен оставаться bounded habit companion; метрика/риск: privacy/trust risk, unsupported-memory-use rate
- [ ] Проверить onboarding/paywall copy на emotional companion language и заменить на utility promise "7 дней действий из книги + проверяемая память по привычке" — зачем: Apple/Siri mainstream signal отделяет полезного assistant от emotional companion; метрика/риск: onboarding conversion, trust complaints, low-trust paywall
- [ ] Реализовать proof paywall как state transition после третьего completed action: `paywall_blocked -> ready_for_proof -> habit_paywall_proof_rendered` с X/Y/Z actions и next 30-day promise — зачем: AI apps хуже удерживают подписчиков без видимого recurring value; метрика/риск: paywall CTR, trial cancellation, activation-to-paywall conversion

## 2026-06-02
- [ ] Добавить `HabitPlanState`/`HabitCheckinState` как явные state transitions (`goal -> trigger -> action_done/action_missed -> friction -> next_micro_action`) — зачем: Phase 8 должен менять состояние поведения, а не генерировать чат; метрика/риск: `book_habit_day1_completed`, D1 return, generic-chat risk
- [ ] Ввести memory write rubric после check-in: сохранять только факты, которые меняют следующий action (`completed_action`, `friction_tag`, `trigger_viability`, `confidence`) — зачем: task-focused memory снижает шум и ложную персонализацию; метрика/риск: D7 retention, memory pollution, wrong-next-action risk
- [ ] Сделать paywall progress artifact после 3 completed actions с перечислением X/Y/Z и next 30-day promise — зачем: RevenueCat-сигнал показывает, что ранний churn трудно вернуть; метрика/риск: paywall CTR, trial cancellation, perceived value
- [ ] Добавить completion-chain dashboard/query: plan created -> Day1 completed -> D1 return -> Day3 completed -> paywall viewed/clicked -> D7 retained — зачем: AI label сам по себе не удерживает, нужен измеримый loop; метрика/риск: retention blind spot, false-positive activation
- [ ] Привязать golden set к paywall copy: для каждой из 3 книг проверить, что первые 3 actions достаточно конкретны, чтобы стать доказательством прогресса на paywall — зачем: monetization copy должен опираться на реальные completed actions; метрика/риск: WTP validation, low-trust paywall

## 2026-06-03
- [ ] Добавить к habit memory event ссылку на исходный check-in/evidence snippet рядом с derived fields — зачем: снизить риск ложной персонализации и потери ground truth; метрика/риск: memory accuracy, wrong-next-action risk
- [ ] Разделить durable memory на append-only события и derived facts с полем `confidence` — зачем: не переписывать профиль пользователя после каждого check-in; метрика/риск: memory drift, faulty update risk, D7 retention
- [ ] В paywall после 3 completed actions показывать конкретные выполненные действия X/Y/Z и next 30-day promise — зачем: AI subscription retention требует visible recurring value, а не generic AI pitch; метрика/риск: paywall CTR, trial cancellation, WTP validation
- [ ] Добавить событие `habit_paywall_proof_rendered` с количеством completed actions и book slug — зачем: проверить, что monetization происходит после value proof; метрика/риск: activation-to-paywall chain, low-trust paywall
- [ ] В golden set пометить каждый generated action как `fact`, `inference` или `unsupported` относительно книги и user goal — зачем: не продавать hallucinated self-help как персонализацию; метрика/риск: trust risk, action quality score

## 2026-06-04
- [ ] Сделать paywall proof renderer, который подставляет 3 completed actions и next 30-day promise вместо generic AI copy — зачем: RevenueCat/consumer AI signals показывают, что retention требует visible recurring value; метрика/риск: `habit_paywall_proof_rendered`, paywall CTR, low-trust paywall
- [ ] Добавить memory-use eval в golden set: next action должен опираться только на confirmed facts или явно помеченные low-confidence inference — зачем: персональная память без проверки может ухудшить action и доверие; метрика/риск: wrong-next-action rate, memory accuracy, D7 retention
- [ ] Ввести prompt memory budget для check-in generation: максимум N relevant events/facts + fallback без long context — зачем: persistent companion должен быть экономичным и предсказуемым; метрика/риск: cost per check-in, latency, context pollution
- [ ] Добавить stable IDs к habit events (`book_slug`, `goal_id`, `trigger_id`, `action_id`, `checkin_id`) — зачем: habit graph должен быть проверяемым и потенциально переносимым, а не чат-логом; метрика/риск: analytics join quality, memory portability, debugging speed
- [ ] Построить completion-chain query `plan_created -> day1_completed -> d1_return -> day3_completed -> paywall_proof_rendered -> paywall_clicked` — зачем: оптимизировать именно behavior loop до монетизации; метрика/риск: activation-to-paywall conversion, retention blind spot

## 2026-06-05
- [ ] Добавить freshness-aware поля в habit memory (`memory_last_synthesized_at`, `source_checkin_id`, `evidence_snippet`, `confidence`) — зачем: свежий OpenAI memory signal показывает, что long-term companion ломается на stale/incorrect memory; метрика/риск: memory accuracy, wrong-next-action rate, trust risk
- [ ] Сделать stale-memory fallback для генерации next action: если memory устарела или low-confidence, использовать только текущие `goal + trigger + book_slug` — зачем: лучше честная неперсонализированная рекомендация, чем ложная персонализация; метрика/риск: unsupported-memory-use rate, D1/D7 retention
- [ ] Добавить `trigger_matched` в check-in и метрику `same_trigger_completed_days / active_days` — зачем: retention для AI companion должен измерять реальный жизненный слот, а не просто DAU; метрика/риск: trigger consistency, habit formation risk
- [ ] В paywall proof sheet показывать remembered context из последних 3 completed actions: действие, триггер, friction/adjustment — зачем: пользователь платит за видимый прогресс и ощущение continuity; метрика/риск: `habit_paywall_proof_rendered`, paywall CTR, low-trust paywall
- [ ] В Day 1–3 UI добавить короткую строку "что SiaMentor помнит со вчера" перед next action — зачем: memory должна быть ощутимой consumer feature, а не скрытым backend; метрика/риск: D1 return, perceived personalization, hallucinated-memory complaints

## 2026-06-06
- [ ] Добавить memory transparency line в Day 2–7: `Помню из прошлого check-in: <evidence_snippet>` + действие "неверно/забыть" — зачем: свежая волна ChatGPT memory показывает, что continuity нужна вместе с контролем пользователя; метрика/риск: memory correction rate, trust complaints, D1/D7 retention
- [ ] Ограничить MVP memory allowlist полями `goal`, `trigger`, `completed_action`, `friction`, `confidence`, `source_checkin_id` — зачем: consumer backlash вокруг persistent memory делает широкий life profile рискованным; метрика/риск: privacy/trust risk, unsupported-memory-use rate
- [ ] Добавить stale-memory UX state: если последний check-in старше N дней, показывать restart/grace-day flow вместо уверенной персонализации — зачем: устаревшая memory хуже неперсонализированного action; метрика/риск: stale-memory usage, wrong-next-action rate, return-after-gap conversion
- [ ] В paywall proof renderer добавить блок "что мы уже знаем, что у тебя работает" из 3 completed actions и triggers — зачем: продавать observable continuity, а не общий AI pitch; метрика/риск: `habit_paywall_proof_rendered`, paywall CTR, perceived value
- [ ] В golden set добавить privacy/trust rubric: next action не должен раскрывать или использовать memory вне habit domain — зачем: доверие D2C companion зависит от узкой и объяснимой памяти; метрика/риск: trust-risk eval failures, hallucinated-memory complaints

## 2026-06-07
- [ ] Разделить habit memory storage на append-only `HabitCheckinEvent` и derived `HabitMemoryFact` с `source_checkin_id` — зачем: свежий backlash вокруг summarized memory показывает, что синтез без evidence снижает доверие; метрика/риск: memory correction rate, unsupported-memory-use rate, wrong-next-action risk
- [ ] Добавить UI affordance `верно / исправить / забыть` рядом с memory transparency line — зачем: OpenAI Memory FAQ закрепляет expectation пользовательского контроля над memory; метрика/риск: correction usage, trust complaints, D1/D7 retention
- [ ] В golden set проверять, что derived memory не обобщает конкретный habit fact в lifestyle/personality summary — зачем: для book-to-habit важен проверяемый trigger/action, а не широкий personal profile; метрика/риск: generalized-memory eval failures, privacy risk
- [ ] Добавить `same_trigger_completed_days / active_days` в completion-chain dashboard — зачем: companion должен менять реальное поведение в повторяемом жизненном слоте, а не только поднимать DAU; метрика/риск: habit formation signal, false-positive retention
- [ ] Переписать paywall proof copy вокруг 3 evidence-backed actions: `ты уже сделал X/Y/Z; следующий 30-day план усилит trigger N` — зачем: value proof понятнее, чем generic AI subscription; метрика/риск: `habit_paywall_proof_rendered`, paywall CTR, WTP validation

## 2026-06-08
- [ ] Добавить `habit_memory_corrected` event для действий `верно / исправить / забыть` — зачем: свежий рынок memory summaries показывает, что контроль пользователя становится trust primitive; метрика/риск: correction rate, trust complaints, unsupported-memory-use rate
- [ ] Ввести eval failure `overgeneralized_memory` для derived facts — зачем: memory не должна превращать конкретный habit evidence в широкий lifestyle/personality profile; метрика/риск: overgeneralization failures, wrong-next-action risk, privacy risk
- [ ] Обновить Day 1-3 golden set рубрикой scaffolded action: один trigger, действие до 10 минут, check-in question, следующий шаг зависит от ответа — зачем: AI tutoring evidence требует помогать думать/действовать, а не просто выдавать совет; метрика/риск: action quality score, D1 return, generic-advice risk
- [ ] Добавить в completion-chain dashboard `habit_memory_corrected`, `overgeneralized_memory_eval_failed`, `same_trigger_completed_days / active_days` — зачем: retention companion надо мерить через качество памяти и повторяемость trigger, а не только через DAU; метрика/риск: habit formation signal, trust metric blind spot
- [ ] В paywall proof renderer показывать `source_checkin_id`-backed actions X/Y/Z и trigger pattern — зачем: paywall после 3 completed actions должен продавать доказанную continuity; метрика/риск: `habit_paywall_proof_rendered`, paywall CTR, WTP validation
- [ ] Протестировать mobile-first экран Day 2 check-in с memory controls `верно / исправить / забыть` перед today's action — зачем: пользователь должен доверять памяти companion до того, как она влияет на следующий habit-шаг; метрика/риск: correction usage, wrong-next-action complaints, D1 return

## 2026-06-09
- [ ] Переписать paywall proof copy как artifact прогресса: `ты сделал X/Y/Z`, `trigger N сработал`, `30-day plan усилит этот ритм` — зачем: свежий consumer AI сигнал показывает WTP, но retention требует видимого recurring value; метрика/риск: `habit_paywall_proof_rendered`, paywall CTR, trial cancellation
- [ ] Добавить в paywall renderer поле `best_trigger_pattern` на базе completed check-ins — зачем: продавать не "AI", а обнаруженный жизненный слот пользователя; метрика/риск: WTP validation, perceived personalization, low-trust paywall
- [ ] Ввести MVP memory domain boundary: запрещать derived facts шире habit-домена (`goal`, `trigger`, `action`, `friction`, `confidence`) — зачем: companion memory должна быть контролируемой и не превращаться в широкий life profile; метрика/риск: privacy/trust risk, overgeneralized_memory eval failures
- [ ] Добавить stale/grace recovery event `habit_restart_after_gap_started` — зачем: RevenueCat/retention signals напоминают, что churn после пропуска надо обрабатывать отдельным loop, а не обычным Day N; метрика/риск: return-after-gap conversion, D7 retention, streak-break churn
- [ ] Проверить Day 1-3 golden set на scaffolded action rubric: trigger выбран пользователем, действие ≤10 минут, есть check-in question, next action зависит от ответа — зачем: AI tutor evidence переносится в habit companion как "помогает действовать", а не "дает совет"; метрика/риск: action quality score, D1 return, generic-advice risk
- [ ] Протестировать mobile-first paywall proof screen после 3 completed actions: evidence cards X/Y/Z + best trigger pattern + 30-day offer — зачем: paywall должен продавать доказанный прогресс, а не generic AI subscription; метрика/риск: `habit_paywall_proof_rendered`, paywall CTR, low-trust paywall

## 2026-06-11
- [ ] Спроектировать и проверить mobile-first Day 2–7 memory trust gate: memory evidence line + `верно / исправить / забыть` перед next action — зачем: цепочка книга -> действие -> привычка должна быть персонализированной, но контролируемой; метрика/риск: `habit_memory_corrected`, daily check-in completion, wrong-next-action complaints
- [ ] Добавить Day 2–7 memory transparency line с действиями `исправить / забыть` — зачем: consumer AI trust сейчас строится вокруг приватной и контролируемой памяти, а не просто "long-term memory"; метрика/риск: `habit_memory_corrected`, trust complaints, wrong-next-action risk
- [ ] Ввести hard allowlist для `HabitMemoryFact`: `goal`, `trigger`, `completed_action`, `friction`, `confidence`, `source_checkin_id` — зачем: EDPS companion signal показывает, что personalization/memory повышает privacy risk; метрика/риск: `memory_outside_habit_domain`, unsupported-memory-use rate, overgeneralized_memory eval failures
- [ ] Добавить в golden set failure case `generic_advice_without_trigger` — зачем: AI tutoring тренд уходит в guided workflows, а book-to-habit должен помогать действовать, не пересказывать книгу; метрика/риск: action quality score, D1 return, generic-advice risk
- [ ] Пересобрать paywall proof renderer вокруг `completed_actions + best_trigger_pattern + friction_adjustment + 30_day_promise` — зачем: AI apps имеют ранний WTP, но слабую retention, поэтому paywall должен продавать видимый recurring value; метрика/риск: `habit_paywall_proof_rendered`, paywall CTR, trial cancellation
- [ ] Добавить `same_trigger_completed_days / active_days` в completion-chain dashboard — зачем: для habit companion важна повторяемость жизненного слота, а не только DAU или число check-in; метрика/риск: habit formation signal, false-positive retention

## 2026-06-10
- [x] Спроектировать mobile-first `Grace restart` экран для пропуска 24–72 часа: evidence snippet + grace day + микро-действие ≤5 минут + calm restart option — зачем: цепочка книга -> действие -> привычка должна восстанавливать пользователя после паузы; метрика/риск: `grace_day_used`, `habit_restart_after_gap_completed`, return-after-gap conversion *(done 2026-06-10: добавлен HTML-макет `design/mockups/grace-restart-mobile.html`)*
- [ ] Добавить в Phase 8 события `grace_day_used`, `habit_restart_after_gap_started`, `habit_restart_after_gap_completed`, `hours_since_missed_checkin` — зачем: streak должен восстанавливать пользователя после пропуска, а не только показывать серию; метрика/риск: return-after-gap conversion, D7 retention, streak-break churn
- [ ] Сделать recovery flow для пропуска до 72 часов: короткий restart экран + сохранение streak через grace day + новый микро-шаг ≤5 минут — зачем: свежие streak/recovery данные указывают, что окно возврата быстро закрывается; метрика/риск: restart completion rate, D3/D7 retention
- [ ] В paywall proof renderer добавить связку `best_trigger_pattern + friction_adjustment + next_30_day_promise` — зачем: RevenueCat показывает высокий AI WTP, но слабую AI retention; платить должны за доказанную continuity, а не за AI-label; метрика/риск: `habit_paywall_proof_rendered`, paywall CTR, refund/trial cancellation
- [ ] Ввести hard domain boundary для memory writes и eval `memory_outside_habit_domain` — зачем: EDPS AI companions signal поднимает privacy risk вокруг personalization/memory; метрика/риск: privacy/trust risk, unsupported-memory-use rate, overgeneralized_memory failures
- [ ] Переписать Day 2-7 check-in prompt как guided action flow: вспомнить evidence snippet, спросить про trigger, выбрать friction, предложить следующий if-then шаг — зачем: AI tutor evidence переносится в habit MVP как scaffolded behavior change; метрика/риск: action quality score, `daily_habit_checkins_completed`, generic-advice risk

## 2026-06-12
- [ ] Зафиксировать inventory текущих экранов для переезда на новый mobile-first макет: onboarding, Day 1 action, Day 2-7 check-in, memory trust gate, recovery, paywall proof — зачем: переезд должен собрать MVP-flow, а не стать визуальным редизайном ради редизайна; метрика/риск: меньше пропущенных состояний, быстрее реализация первой недели
- [ ] Собрать flow map нового макета `book -> action -> check-in -> memory -> recovery -> paywall proof` с данными `goal`, `trigger`, `completed_action`, `friction`, `confidence`, `source_checkin_id` — зачем: понять, где memory влияет на следующий шаг и где пользователь её контролирует; метрика/риск: wrong-next-action risk, implementation clarity
- [ ] Доработать новый layout shell под ежедневный habit loop: основной action surface, progress area, место для memory evidence и CTA следующего шага — зачем: интерфейс должен ощущаться как companion habit system, а не generic chat; метрика/риск: D1/D7 retention, generic-chat risk
- [ ] Встроить Day 2-7 memory trust gate в новый макет перед today's action: evidence line + `верно / исправить / забыть` — зачем: переезд на новый макет должен сразу нести controllable memory как trust primitive; метрика/риск: `habit_memory_corrected`, trust complaints, wrong-next-action complaints
- [ ] Встроить recovery state 24-72h в новый макет: calm restart + grace day + evidence snippet + микро-действие ≤5 минут — зачем: пользователь должен возвращаться после пропуска внутри того же MVP-flow; метрика/риск: `habit_restart_after_gap_completed`, return-after-gap conversion, D7 retention
- [ ] Встроить paywall proof state после 3 completed actions в новый макет: actions X/Y/Z + cautious trigger pattern + friction adjustment + CTA 30-day plan — зачем: monetization должна появляться после доказанного прогресса в том же интерфейсном пути; метрика/риск: `habit_paywall_proof_rendered`, paywall CTR, trial cancellation
- [ ] Проверить новый mobile-first MVP-flow end-to-end на перегруз, ранний paywall, ложную уверенность memory и понятность следующего шага — зачем: перед implementation надо поймать продуктовые провалы на макете; метрика/риск: usability risk, wrong-pattern complaints, low-trust paywall
- [ ] Собрать mobile-first Day 2–7 memory trust gate: evidence line `Помню: trigger/action/friction` + `верно / исправить / забыть` перед today's action — зачем: consumer AI trust сейчас строится на контролируемой памяти, а не на обещании "AI помнит"; метрика/риск: `habit_memory_corrected`, unsupported-memory-use rate, wrong-next-action complaints
- [ ] Ввести eval failure `memory_outside_habit_domain` для любых derived facts вне `goal`, `trigger`, `completed_action`, `friction`, `confidence`, `source_checkin_id` — зачем: EDPS companion risk показывает, что широкая персонализация превращает MVP в privacy-sensitive profile; метрика/риск: privacy/trust risk, overgeneralized_memory failures
- [ ] Обновить Day 1–3 golden set рубрикой guided action: выбранный trigger, действие ≤10 минут, check-in question, next action зависит от ответа — зачем: AI tutoring сигнал переносится в book-to-habit как scaffolded behavior change, а не пересказ книги; метрика/риск: action quality score, `book_habit_day1_completed`, generic-advice risk
- [ ] Пересобрать paywall proof renderer вокруг `completed_actions + best_trigger_pattern + friction_adjustment + next_30_day_promise` — зачем: RevenueCat 2026 показывает retention risk у AI apps, поэтому paywall должен продавать visible continuity после 3 actions; метрика/риск: `habit_paywall_proof_rendered`, paywall CTR, trial cancellation
- [ ] Добавить recovery-chain events `hours_since_missed_checkin`, `grace_day_used`, `habit_restart_after_gap_completed` и dashboard-срез return-after-gap — зачем: streak без восстановления не удерживает пользователя после первого пропуска; метрика/риск: restart completion rate, D7 retention, streak-break churn
- [ ] Протестировать mobile-first paywall proof после 3 completed actions: completed actions X/Y/Z + осторожный best trigger pattern + friction adjustment + CTA 30-day plan — зачем: paywall должен продавать доказанную цепочку книга -> действие -> привычка, а не generic AI; метрика/риск: `habit_paywall_proof_rendered`, paywall CTR, trial cancellation, wrong-pattern complaints

## 2026-06-13
- [ ] Переформулировать onboarding/paywall copy из broad AI companion в bounded habit companion: "7 дней действий из книги + проверяемая память по привычке" — зачем: свежий Apple/Siri signal усиливает boundaries/privacy, а не эмоциональную зависимость; метрика/риск: trust complaints, low-trust paywall, onboarding conversion
- [ ] Добавить Day 2-7 memory evidence line с тремя действиями `верно / исправить / забыть` перед today's action — зачем: privacy-first consumer AI требует контролируемой памяти, иначе персонализация выглядит как слежка; метрика/риск: `habit_memory_corrected`, unsupported-memory-use rate, wrong-next-action complaints
- [ ] Ввести eval `memory_outside_habit_domain` и hard allowlist для derived facts: `goal`, `trigger`, `completed_action`, `friction`, `confidence`, `source_checkin_id` — зачем: EDPS companion risk показывает опасность continuous profiling; метрика/риск: privacy/trust risk, overgeneralized_memory failures
- [ ] Собрать proof paywall state после 3 completed actions: X/Y/Z actions + cautious trigger pattern + friction adjustment + next 30-day promise — зачем: RevenueCat показывает retention weakness у AI apps, значит paywall должен продавать visible recurring value; метрика/риск: `habit_paywall_proof_rendered`, paywall CTR, trial cancellation
- [ ] Добавить recovery-first streak dashboard: `hours_since_missed_checkin`, `grace_day_used`, `habit_restart_after_gap_completed`, return-after-gap conversion — зачем: streak break может стать quit moment, если нет отдельного восстановления; метрика/риск: D7 retention, streak-break churn, restart completion rate
- [ ] Спроектировать gated paywall state `paywall_blocked` до 3 completed actions и `ready_for_proof` после 3 actions — зачем: monetization должна следовать за доказанной цепочкой книга -> действие -> привычка, а не появляться как generic AI subscription; метрика/риск: `habit_paywall_proof_rendered`, paywall CTR, early-paywall churn

## 2026-06-14
- [ ] Добавить Day 2-7 memory evidence line в основной habit surface: `Помню: <trigger/action/friction>` + `верно / исправить / забыть` — зачем: Apple/Siri mainstream signal усиливает privacy-bounded personal context; метрика/риск: `habit_memory_corrected`, unsupported-memory-use rate, wrong-next-action complaints
- [ ] Собрать cohort funnel dashboard `plan_created -> day1_completed -> d1_return -> day3_completed -> paywall_proof_rendered -> d7_retained` — зачем: Appcues retention guidance напоминает смотреть drop-off по когортам, а не только NSM; метрика/риск: activation-to-paywall conversion, D7 retention, retention blind spot
- [ ] Переписать paywall copy вокруг proof loop: X/Y/Z completed actions, cautious trigger pattern, friction adjustment, next 30-day promise — зачем: RevenueCat показывает, что AI subscription retention слабее non-AI; метрика/риск: `habit_paywall_proof_rendered`, paywall CTR, trial cancellation
- [ ] Встроить recovery restart после 24-72h пропуска: evidence snippet + grace day + микро-действие ≤5 минут + calm CTA — зачем: streak safety valves снижают риск quit moment после первого пропуска; метрика/риск: `grace_day_used`, `habit_restart_after_gap_completed`, return-after-gap conversion
- [ ] Ограничить product copy формулой bounded habit companion, не emotional/life companion — зачем: свежие Apple/Siri boundaries показывают, что consumer trust строится на полезности и ограничениях; метрика/риск: onboarding conversion, trust complaints, low-trust paywall
- [ ] Ввести monetization gate `paywall_blocked` до 3 completed actions и `ready_for_proof` после 3 actions — зачем: paywall должен следовать за доказанной цепочкой книга -> действие -> привычка; метрика/риск: early-paywall churn, `habit_paywall_proof_rendered`, paywall CTR

## 2026-06-15
- [ ] Протестировать first-session action lock: onboarding не завершается без `book_habit_day1_completed`, а `paywall_blocked` держится до 3 completed actions — зачем: пользователь должен почувствовать цепочку книга -> действие -> привычка до монетизации; метрика/риск: activation rate, D1 return, early-paywall churn
- [ ] Перестроить first-session flow так, чтобы пользователь завершал `book_habit_day1_completed` до выхода из сессии — зачем: retention benchmark показывает, что Day 7 растет, когда core value action случается сразу; метрика/риск: activation rate, D1 return, plan-created-without-action drop-off
- [ ] Добавить onboarding boundary promise: "помним только цель, триггер, выполненное действие и препятствие для следующего шага" — зачем: свежий Siri/consumer AI signal усиливает trust через ограниченный personal context; метрика/риск: onboarding conversion, trust complaints, unsupported-memory-use rate
- [ ] Переписать Day 2-7 check-in как guided action flow: подтвердить прошлый evidence snippet, выбрать `trigger_matched`, выбрать friction и получить следующий if-then шаг — зачем: context-aware AI должен доводить до behavior execution, а не вести generic chat; метрика/риск: `daily_habit_checkins_completed`, action quality score, generic-chat risk
- [ ] Ввести hard gate `paywall_blocked` до 3 completed actions и `ready_for_proof` после третьего completed action — зачем: paywall должен продавать доказанный прогресс, а не обещание AI; метрика/риск: early-paywall churn, `habit_paywall_proof_rendered`, paywall CTR
- [ ] Добавить eval/event `memory_outside_habit_domain` для любых memory writes вне allowlist `goal/trigger/completed_action/friction/confidence/source_checkin_id` — зачем: AI companion privacy risk растет именно вокруг широкой personalization/memory; метрика/риск: privacy/trust risk, overgeneralized_memory failures
- [ ] Проверить marketing copy на companion-category risk: заменить broad/emotional companion формулировки на bounded habit companion — зачем: рынок и регуляторный контекст двигаются к clear boundaries; метрика/риск: low-trust paywall, policy/reputation risk, onboarding clarity

## 2026-06-16
- [ ] Проверить единый mobile-first habit surface `book context -> today's action -> memory receipt -> streak/grace -> progress to proof paywall` — зачем: пользователь должен видеть один ежедневный ритуал книга -> действие -> привычка, а не набор разрозненных экранов; метрика/риск: `daily_habit_checkins_completed`, D1/D7 retention, generic-chat risk
- [ ] Добавить visible memory receipt в Day 2-7: `Помню: <trigger/action/friction>` + `верно / исправить / забыть` — зачем: privacy-first AI все равно может ощущаться creepy без контроля; метрика/риск: `habit_memory_corrected`, trust complaints, wrong-next-action complaints
- [ ] Свести main habit surface в один экран: today's action, evidence line, streak/grace state и progress to proof paywall — зачем: AI должен быть встроен в выполнение привычки, а не спрятан в отдельный чат; метрика/риск: `daily_habit_checkins_completed`, D1/D7 retention, generic-chat risk
- [ ] Переписать marketing/paywall copy в формулу bounded habit companion: "7 дней действий из книги + проверяемая память по привычке" — зачем: свежий Siri signal уводит рынок от emotional companion category к utility/trust; метрика/риск: onboarding conversion, low-trust paywall, policy/reputation risk
- [ ] Реализовать monetization guard: `paywall_blocked` до 3 completed actions, затем `habit_paywall_proof_rendered` с X/Y/Z actions и next 30-day promise — зачем: AI apps хуже удерживают подписчиков без видимого recurring value; метрика/риск: paywall CTR, trial cancellation, early-paywall churn
- [ ] Добавить dashboard-срез `plan_created -> book_habit_day1_completed -> d1_return -> day3_completed -> habit_paywall_proof_rendered -> d7_retained` — зачем: проверять behavior loop фактами, а не количеством созданных планов; метрика/риск: activation-to-paywall conversion, retention blind spot

## 2026-06-17
- [ ] Протестировать post-action receipt после `Сделал`: book anchor + completed action + bounded memory receipt + progress `1/3` до proof paywall — зачем: выполнение должно явно превращаться в повторяемую привычку до монетизации; метрика/риск: `book_habit_day1_completed`, `habit_memory_corrected`, D1 return, early-paywall churn

## 2026-06-18
- [ ] Спроектировать и проверить Day 2 anti-idle ritual state в основном habit surface: receipt прошлого действия + trigger confirm + микро-действие 5-10 минут + progress `1/3` до proof paywall — зачем: второй день должен закреплять книгу -> действие -> привычка, а не превращаться в answer machine; метрика/риск: `daily_habit_checkins_completed`, same-trigger completion, generic-chat risk
- [ ] Заблокировать завершение первой сессии без `book_habit_day1_completed` или явного "сделаю позже" recovery hook — зачем: свежий Stanford/The 74 сигнал показывает, что доступ к AI tutor сам по себе дает 2-5 минут usage и не создает привычку; метрика/риск: activation rate, plan-created-without-action drop-off, D1 return
- [ ] Добавить anti-idle Day 2 ritual: evidence receipt прошлого действия + один if-then trigger + микро-действие до 10 минут + check-in question — зачем: book-to-habit companion должен возвращать пользователя к guided practice, а не становиться answer machine; метрика/риск: `daily_habit_checkins_completed`, same-trigger completion, generic-chat risk
- [ ] В golden set добавить eval `guided_practice_not_answer_machine` — зачем: AI output должен вести к действию из книги, а не к длинному объяснению или совету; метрика/риск: action quality score, unsupported advice rate, D7 retention
- [ ] Ограничить default actions для adult D2C users до 5-10 минут и обязательного trigger binding — зачем: adult learning constraints требуют small-contextual actions, иначе план конкурирует с рабочим днем; метрика/риск: first-action completion, action-too-large failures, D1 return
- [ ] Добавить companion-risk eval: no emotional dependency language, no broad personality profiling, no memory outside `goal/trigger/completed_action/friction/confidence/source_checkin_id` — зачем: companion safety/memory signals требуют bounded habit memory; метрика/риск: trust complaints, `memory_outside_habit_domain`, overgeneralized_memory failures
- [ ] Проверить proof paywall state после третьего completed action: X/Y/Z actions + trigger pattern + friction adjustment + next 30-day promise — зачем: AI apps retention слабее без visible recurring value; метрика/риск: `habit_paywall_proof_rendered`, paywall CTR, trial cancellation

## 2026-06-19
- [ ] Протестировать `ready_for_proof` paywall receipt на weak-pattern copy: три completed actions + осторожный trigger pattern + friction adjustment + free continue path — зачем: paywall должен доказывать ценность без ложной уверенности и без раннего AI subscription pitch; метрика/риск: `habit_paywall_proof_rendered`, paywall CTR, wrong-pattern complaints, trial cancellation
- [ ] Реализовать proof paywall evidence contract: `completed_actions[3]`, `best_trigger_pattern`, `friction_adjustment`, `next_30_day_promise` — зачем: RevenueCat показывает retention weakness AI apps, поэтому paywall должен продавать видимый recurring value; метрика/риск: `habit_paywall_proof_rendered`, paywall CTR, trial cancellation
- [ ] Добавить cohort funnel `plan_created -> book_habit_day1_completed -> d1_return -> day3_completed -> habit_paywall_proof_rendered -> trial_started -> d30_paid_retained` — зачем: AI conversion premium может скрывать churn problem, если смотреть только paywall click; метрика/риск: activation-to-paid-retention conversion, retention blind spot
- [ ] Встроить bounded memory receipt в Day 2-7: `source_checkin_id`, `evidence_snippet`, `confidence`, действия `верно / исправить / забыть` — зачем: companion/memory safety signals требуют прозрачной и исправляемой памяти; метрика/риск: `habit_memory_corrected`, wrong-next-action complaints, trust complaints
- [ ] Провести copy check на формулу bounded habit companion: убрать emotional dependency language, broad personality profiling и обещания "знаю тебя глубоко" — зачем: companion category несет safety/reputation risk, а SiaMentor должен быть utility-first; метрика/риск: low-trust paywall, policy/reputation risk, onboarding clarity
- [ ] Добавить return-after-gap recovery state после пропуска: calm receipt + grace day + микро-действие ≤5 минут + восстановление progress to proof paywall — зачем: пропуск streak может стать quit moment до монетизации; метрика/риск: `habit_restart_after_gap_completed`, return-after-gap conversion, D7 retention

## 2026-06-20
- [ ] Зафиксировать privacy receipt contract для Day 2-7: `source_checkin_id`, `evidence_snippet`, `confidence`, `верно / исправить / забыть` — зачем: свежий EDPS companion/privacy signal усиливает ожидание контролируемой personalization; метрика/риск: `habit_memory_corrected`, wrong-next-action complaints, trust complaints
- [ ] Спроектировать Day 2-7 privacy receipt как gate перед today's action: подтверждение/исправление/забывание памяти должно открывать следующий шаг 5-10 минут — зачем: пользователь должен видеть, как книга -> вчерашнее действие -> проверенная память превращается в привычку без ощущения скрытого профилирования; метрика/риск: `habit_memory_corrected`, `daily_habit_checkins_completed`, wrong-next-action complaints
- [ ] Добавить eval `guided_practice_not_answer_machine`: ответ должен содержать trigger, действие ≤10 минут, check-in question и next step based on response — зачем: AI tutor value переносится в SiaMentor как сопровождение действия, а не пересказ книги; метрика/риск: action quality score, unsupported advice rate, D7 retention
- [ ] Проверить first-session action lock: нельзя завершить onboarding без `book_habit_day1_completed` или явного recovery hook "сделаю позже" — зачем: habit app контекст быстро проигрывает рабочим и бытовым отвлечениям; метрика/риск: activation rate, plan-created-without-action drop-off, D1 return
- [ ] Добавить в proof paywall free continue path рядом с paid CTA — зачем: paywall после 3 actions должен доказывать ценность без ложной уверенности и не ломать habit loop; метрика/риск: `habit_paywall_proof_rendered`, paywall CTR, early-paywall churn, trust complaints
- [ ] Расширить dashboard trust-срезом `habit_memory_corrected`, `memory_outside_habit_domain`, `guided_practice_not_answer_machine_failed`, `free_continue_clicked` — зачем: D2C companion risk проявляется в памяти, качестве действия и давлении paywall, а не только в DAU; метрика/риск: trust metric blind spot, retention blind spot, policy/reputation risk

## 2026-06-21
- [ ] Протестировать state `memory_corrected_next_action`: после исправления receipt пользователь сразу получает микро-действие 5-10 минут, а proof paywall считает только completed actions — зачем: correction должна возвращать цепочку книга -> действие -> привычка, а не становиться настройкой профиля; метрика/риск: `habit_memory_corrected`, `daily_habit_checkins_completed`, wrong-next-action complaints
- [ ] Сделать Day 2-7 memory receipt обязательным gate перед next action: `source_checkin_id`, snippet, confidence, `верно / исправить / забыть` — зачем: свежий EDPS/FPF privacy контекст усиливает ожидание контролируемой memory в companion UX; метрика/риск: `habit_memory_corrected`, wrong-next-action complaints, privacy/trust risk
- [ ] Добавить golden-set fail case `answer_machine_without_practice`: нет trigger, действия ≤10 минут, check-in question и next step from response — зачем: learning companion research предупреждает о риске episodic answer-machine вместо повторяемой guided practice; метрика/риск: action quality score, `guided_practice_not_answer_machine_failed`, D7 retention
- [ ] Зафиксировать proof-paywall contract как typed object: `completed_actions[3]`, `best_trigger_pattern`, `friction_adjustment`, `next_30_day_promise`, `free_continue_available` — зачем: RevenueCat показывает AI retention gap, значит paywall должен продавать observable behavior proof; метрика/риск: `habit_paywall_proof_rendered`, paywall CTR, trial cancellation, early-paywall churn
- [ ] Провести copy boundary pass для onboarding/paywall: заменить emotional companion обещания на "7 дней действий из книги + проверяемая память по привычке" — зачем: Siri/companion boundary signal показывает ценность utility-first позиционирования; метрика/риск: onboarding conversion, low-trust paywall, policy/reputation risk
- [ ] Добавить dashboard trust slice: `habit_memory_corrected`, `memory_outside_habit_domain`, `wrong_next_action_reported`, `free_continue_clicked` — зачем: privacy и paywall pressure риски не видны в обычном DAU/funnel dashboard; метрика/риск: trust metric blind spot, retention blind spot, hidden churn driver
- [ ] Протестировать free continue path после proof paywall: после 3 completed actions пользователь видит receipt, платный 30-дневный CTA и бесплатное действие на сегодня без потери progress — зачем: paywall должен монетизировать доказанную цепочку книга -> действие -> привычка, но не ломать доверие; метрика/риск: `habit_paywall_proof_rendered`, `free_continue_clicked`, early-paywall churn, D7 retention
- [ ] Протестировать post-third-action paywall surface: proof receipt + cautious trigger pattern + paid 30-day CTA + free continue action — зачем: монетизация должна появляться после доказанной цепочки книга -> действие -> привычка, но не ломать ежедневный ритуал; метрика/риск: `habit_paywall_proof_rendered`, `free_continue_clicked`, early-paywall churn, trust complaints

## 2026-06-25
- [ ] Проверить free continue после proof paywall как не-блокирующий continuation state: paid CTA + бесплатное действие 5-10 минут + сохранение progress — зачем: монетизация должна появляться после доказанной цепочки книга -> действие -> привычка, но не превращать третий completed action в churn point; метрика/риск: `free_continue_clicked`, `next_action_completed_after_free_continue`, early-paywall churn, D7 retention

## 2026-06-26
- [ ] Протестировать состояние `next_action_after_free_continue`: после proof paywall пользователь выбирает бесплатное продолжение, получает действие 5-10 минут из той же книги, progress receipt сохраняется — зачем: paywall после 3 completed actions должен монетизировать доказанную цепочку книга -> действие -> привычка, не ломая ежедневный loop; метрика/риск: `free_continue_clicked`, `free_action_completed_after_paywall`, early-paywall churn, D7 retention

## 2026-06-27
- [ ] Проверить состояние `proof_paywall_with_today_action`: после третьего completed action показать proof receipt, paid 30-day CTA и видимое бесплатное действие на сегодня — зачем: paywall должен монетизировать доказанную цепочку книга -> действие -> привычка, не превращая третий успех в churn point; метрика/риск: `habit_paywall_proof_rendered`, `free_continue_clicked`, `free_action_completed_after_paywall`, D7 retention

## 2026-06-28
- [ ] Проверить Day 2 return state `d2_return_from_action_receipt`: вчерашний receipt + trigger/friction + today's action 5-10 минут + progress `2/3` после completion — зачем: пользователь должен продолжать цепочку книга -> действие -> привычка, а не возвращаться к абстрактному плану или LMS-пересказу; метрика/риск: `d1_return_from_action_receipt`, `day2_action_completed`, D7 retention, generic-chat risk
- [ ] Добавить fail state `plan_created_without_day1_action` в habit eval/golden set — зачем: свежие AI tutor данные показывают, что access и план не равны meaningful use; метрика/риск: activation inflation, `minutes_to_first_action_completed`, D1 return
- [ ] Протестировать Day 2 return screen от receipt, а не от abstract plan: показать вчерашнее действие, trigger, friction и next 5-10 minute step — зачем: book-to-habit loop должен возвращать пользователя к уже начатой цепочке, а не к пересказу книги; метрика/риск: `d1_return_from_action_receipt`, `daily_habit_checkins_completed`, wrong-next-action complaints
- [ ] Переписать memory copy boundary на utility-first формулу: "помню вчерашний шаг/препятствие" вместо emotional companion language — зачем: companion privacy/safety контекст повышает цену широкой персонализации; метрика/риск: trust complaints, `memory_outside_habit_domain`, low-trust paywall
- [ ] Проверить proof paywall continuation после бесплатного действия: receipt + paid CTA + free action completion сохраняют progress chain — зачем: AI apps имеют retention gap, поэтому paywall не должен превращать третий успех в churn point; метрика/риск: `free_continue_clicked`, `free_action_completed_after_paywall`, trial cancellation, D7 retention
- [ ] Добавить early recovery instrumentation `hours_since_missed_checkin` и `habit_restart_after_gap_completed` в Phase 8 dashboard — зачем: окно возврата после пропуска быстро закрывается, а grace day должен спасать habit loop до отказа пользователя; метрика/риск: return-after-gap conversion, streak-break churn, D7 retention

## 2026-06-29
- [ ] Заблокировать successful onboarding без `book_habit_day1_completed` или явного `do_later_recovery_hook` — зачем: свежий AI tutoring evidence показывает, что доступ/план не гарантируют meaningful use; метрика/риск: activation inflation, `minutes_to_first_action_completed`, D1 return
- [ ] Спроектировать `missed_checkin_recovery_nudge`: evidence snippet из последнего действия + один next action 5-10 минут + receipt/progress после выполнения — зачем: пропуск check-in должен возвращать пользователя в цепочку книга -> действие -> привычка, а не открывать generic chat; метрика/риск: `habit_nudge_clicked`, `habit_restart_after_gap_completed`, notification fatigue
- [ ] Пересобрать Day 2 return как receipt-first экран: вчерашний action receipt, trigger, friction, today's action 5-10 минут и progress `2/3` — зачем: habit loop должен продолжать начатое действие, а не возвращать пользователя к пересказу книги; метрика/риск: `d2_return_from_action_receipt`, `day2_action_completed`, wrong-next-action complaints
- [ ] Добавить outreach-style nudge для missed/idle check-in: короткое сообщение с evidence snippet и одним next action, без generic chat opening — зачем: chatbot outreach работает, когда ведет к поддержке/действию, а не к свободной беседе; метрика/риск: `habit_nudge_clicked`, `habit_restart_after_gap_completed`, notification fatigue
- [ ] Расширить Phase 8 dashboard trust/retention срезом `plan_created_without_day1_action`, `d2_return_from_action_receipt`, `memory_outside_habit_domain`, `free_action_completed_after_paywall` — зачем: главные риски D2C companion находятся в fake activation, wrong memory и paywall churn; метрика/риск: hidden churn driver, trust complaints, early-paywall churn
- [ ] Провести copy boundary pass по onboarding/memory/paywall: заменить emotional companion обещания на utility-first "7 дней действий из книги + исправляемая память" — зачем: companion privacy/safety сигналы делают broad intimacy positioning рискованным; метрика/риск: trust complaints, low-trust paywall, policy/reputation risk

## 2026-06-30
- [ ] Зафиксировать contract для `missed_checkin_recovery_nudge`: last receipt snippet + одно действие 5-10 минут + completion receipt + progress restore — зачем: июньский Duolingo streak revival показывает, что recovery должен возвращать momentum через действие, а не просто отменять потерю streak; метрика/риск: `habit_nudge_clicked`, `habit_restart_after_gap_completed`, notification fatigue, D7 retention
- [ ] Протестировать recovery state `recovery_action_completed`: после пропуска показать receipt прошлого действия, одно действие 5-10 минут и progress restore `2/3` или `3/3` — зачем: missed check-in должен возвращать пользователя в цепочку книга -> действие -> привычка, а не открывать generic chat или стыдить за streak; метрика/риск: `habit_restart_after_gap_completed`, `recovery_action_completed`, notification fatigue, D7 retention
- [ ] Добавить dashboard-срез fake activation: `plan_created_without_day1_action`, `minutes_to_first_action_completed`, `book_habit_day1_completed / plan_created` — зачем: consumer AI spend растет, но novelty не равна retention; метрика/риск: activation inflation, D1 return, paid curiosity without habit
- [ ] Протестировать proof paywall как non-blocking continuation: после 3 completed actions показать receipt, paid 30-day CTA и бесплатное действие на сегодня — зачем: RevenueCat/TechCrunch retention gap у AI apps требует сохранять habit loop даже в момент монетизации; метрика/риск: `habit_paywall_proof_rendered`, `free_continue_clicked`, `free_action_completed_after_paywall`, early-paywall churn
- [ ] Сделать editable memory receipt перед Day 2-7 action: `Помню: <completed_action> / <trigger> / <friction>` + `верно / исправить / забыть` — зачем: companion memory становится ценностью только если пользователь видит и контролирует, что именно хранится; метрика/риск: `habit_memory_corrected`, `memory_deleted_by_user`, wrong-next-action complaints, trust complaints
- [ ] Добавить eval fail case `generic_ai_plan_without_practice`: план есть, но нет trigger, действия <=10 минут, check-in question и next action from response — зачем: personalized tutor value появляется через guided practice, а не answer-machine summary; метрика/риск: action quality score, `guided_practice_not_answer_machine_failed`, D7 retention

## 2026-07-01
- [ ] Ввести hard activation gate `book_habit_day1_completed_required`: onboarding считается успешным только после первого действия <=10 минут или явного `do_later_recovery_hook` — зачем: AI tutor signal показывает, что meaningful use начинается с guided practice, а не с плана; метрика/риск: `book_habit_day1_completed / plan_created`, `minutes_to_first_action_completed`, fake activation
- [ ] Протестировать экран `plan_ready_action_required`: после книги и цели пользователь видит одно действие 5-10 минут, progress `0/3` и не получает onboarding success до `Сделал` — зачем: первый экран должен запускать цепочку книга -> действие -> привычка, а не завершать ее красивым планом; метрика/риск: `book_habit_day1_completed_required`, `plan_created_without_day1_action`, D1 return
- [ ] Собрать recovery-by-action flow `missed_checkin_recovery_nudge -> recovery_action_completed -> progress_restored`: last receipt snippet, одно действие 5-10 минут, completion receipt — зачем: Duolingo streak revival подтверждает, что восстановление должно возвращать momentum через действие; метрика/риск: `habit_nudge_clicked`, `habit_restart_after_gap_completed`, notification fatigue, D7 retention
- [ ] Провести boundary copy pass для memory surfaces: заменить broad companion формулировки на "помню цель, trigger, выполненное действие и препятствие для следующего шага" — зачем: AI companion regulation signal делает broad emotional memory рискованной; метрика/риск: `memory_outside_habit_domain`, `habit_memory_corrected`, trust complaints
- [ ] Добавить paywall continuation dashboard: `habit_paywall_proof_rendered`, `free_continue_clicked`, `free_action_completed_after_paywall`, `trial_cancelled`, D7 retained — зачем: RevenueCat retention gap требует видеть, не ломает ли proof paywall habit loop; метрика/риск: early-paywall churn, D7 retention, paid curiosity without habit
- [ ] Добавить eval fail case `pretty_plan_no_action`: 7-day plan создан, но нет completed first action, trigger и check-in question — зачем: SiaMentor должен быть book-to-habit companion, а не генератор красивого плана; метрика/риск: activation inflation, action quality score, generic-chat risk

## 2026-07-02
- [ ] Протестировать состояние `book_focus_locked`: после выбора книги пользователь фиксирует один тезис `идея -> поведение`, затем получает первое действие <=10 минут и progress `0/3` — зачем: book-to-habit loop должен начинаться с применимого фокуса, а не с широкого плана; метрика/риск: `book_focus_locked`, `book_habit_day1_completed`, activation inflation, generic-plan risk

## 2026-07-03
- [ ] Протестировать состояние `first_action_checkin`: после `Сделал` показать receipt книги, тезиса, выполненного действия, trigger и progress `1/3`, затем закрыть сессию без generic chat — зачем: первое действие должно превращаться в начало привычки и доказательство для будущего paywall; метрика/риск: `book_habit_day1_completed`, D1 return, `plan_created_without_day1_action`, early-paywall churn

## 2026-07-04
- [x] Ввести `habit_outcome_memory` только после completed/recovered action: `completed_action`, `trigger`, `friction`, `next_adjustment`, `source_checkin_id`, `confidence` — зачем: свежий continual-learning signal поддерживает structured outcomes вместо broad companion memory; метрика/риск: `habit_memory_written_after_action`, `memory_outside_habit_domain`, wrong-next-action complaints *(done 2026-07-04: добавлен typed outcome-memory receipt и PostHog event после completed action; commit 1a7c02d)*
- [ ] Протестировать `two_day_reanchor_needed`: после двух missed check-ins уменьшить действие до <=5 минут, задать один friction-вопрос и переписать if-then trigger — зачем: повторный пропуск чаще означает плохой habit design, а не нехватку мотивации; метрика/риск: `two_day_reanchor_shown`, `habit_restart_after_gap_completed`, notification fatigue, D7 retention
- [ ] Сделать proof paywall non-blocking continuation: после 3 completed actions показать paid CTA и одновременно оставить бесплатное действие на сегодня с receipt/progress — зачем: AI subscription retention gap делает paywall quit-risk, если он ломает habit loop; метрика/риск: `habit_paywall_proof_rendered`, `free_continue_clicked`, `free_action_completed_after_paywall`, trial cancellation
- [ ] Добавить copy boundary в memory receipt: "помню только действия из этой привычки" + `верно / исправить / забыть` — зачем: privacy/profiling pressure вокруг consumer AI усиливает ценность контролируемой памяти; метрика/риск: `habit_memory_corrected`, `memory_deleted_by_user`, trust complaints, low-trust paywall
- [ ] Добавить re-anchor contract после двух пропусков: last receipt + один friction-вопрос + действие <=5 минут + переписанный if-then trigger — зачем: recovery должен чинить habit design и возвращать цепочку книга -> действие -> привычка до paywall; метрика/риск: `two_day_reanchor_shown`, `reanchor_action_completed`, D7 retention, early-paywall pressure

## 2026-07-05
- [x] Реализовать non-blocking proof paywall state после 3 completed actions: receipt/progress `3/3`, paid 30-day CTA и бесплатное действие на сегодня на том же экране — зачем: RevenueCat/TechCrunch показывают retention gap у AI subscriptions, paywall не должен ломать habit loop; метрика/риск: `habit_paywall_proof_rendered`, `free_continue_clicked`, `free_action_completed_after_paywall`, trial cancellation *(done 2026-07-05: proof paywall/free-continue state доступен как Next route `/[locale]/habits/proof`, build подтверждает `/ru/habits/proof` и `/en/habits/proof`; commit 8cc1594)*
- [ ] Добавить post-paywall continuation dashboard: cohort `habit_paywall_proof_rendered -> free_continue_clicked -> free_action_completed_after_paywall -> d7_retained -> trial_started` — зачем: CTR paywall без продолжения привычки может быть vanity metric; метрика/риск: early-paywall churn, paid curiosity without habit, D7 retention
- [ ] Провести copy pass по слову companion: заменить broad/emotional обещания на utility-first "помогаю 7 дней внедрять идеи книги в действия" — зачем: regulatory pressure вокруг AI companions усиливает риск broad intimacy positioning; метрика/риск: trust complaints, low-trust paywall, policy/reputation risk
- [ ] Встроить editable memory receipt перед следующим действием: `Помню: <trigger/action/friction>` + `верно / исправить / забыть`, затем next if-then action — зачем: memory ценна только если пользователь видит, контролирует и ощущает ее влияние на следующий шаг; метрика/риск: `habit_memory_corrected`, `memory_deleted_by_user`, wrong-next-action complaints
- [ ] Закрепить recovery-by-action contract для missed check-in: last receipt snippet + действие <=5 минут + completion receipt + progress restore — зачем: streak recovery должен возвращать momentum через действие, а не просто прощать пропуск; метрика/риск: `habit_restart_after_gap_completed`, `recovery_action_completed`, notification fatigue, D7 retention
- [ ] Протестировать `proof_paywall_free_action`: после третьего completed action paid CTA и бесплатное действие остаются на одном экране, а `paywall_dismissed_no_action` считается churn-risk — зачем: paywall должен монетизировать доказанную цепочку книга -> действие -> привычка, не превращая третий успех в конец loop; метрика/риск: `habit_paywall_proof_rendered`, `free_continue_clicked`, `paywall_dismissed_no_action`, D7 retention

## 2026-07-06
- [x] Протестировать `memory_receipt_gate_before_next_action`: перед Day 2-7 action показать receipt книги/вчерашнего действия/trigger/friction с `Верно / Исправить / Забыть`, а paywall proof после 3 действий считать только verified receipts — зачем: пользователь должен видеть контролируемую цепочку книга -> действие -> привычка до монетизации; метрика/риск: `habit_memory_corrected`, `memory_deleted_by_user`, `next_action_from_verified_memory`, wrong-next-action complaints *(done 2026-07-06: добавлен Day 2 memory receipt gate, события verify/correct/forget и typed proof eligibility только для verified receipt; commit 440d668)*

## 2026-07-07
- [x] Добавить acceptance test `next_action_uses_verified_memory`: Day 2-7 action должен явно использовать verified receipt прошлого дня (`trigger`, `friction`, `completed_action`) — зачем: AI tutor/agent memory ценна только если меняет следующее поведение, а не просто хранит профиль; метрика/риск: `next_action_from_verified_memory`, action quality score, wrong-next-action complaints *(done 2026-07-07: Day 2 next-action bridge теперь явно показывает completed action, trigger и friction из receipt; добавлен Playwright acceptance `habit-verified-memory-next-action.spec.ts`; commit 28d17f9)*
- [ ] Переписать proof paywall evidence copy: показать 3 completed actions, verified receipts и конкретное обещание 30-day continuation — зачем: AI subscription retention gap делает novelty-paywall рискованным без доказанного прогресса; метрика/риск: `habit_paywall_proof_rendered`, `trial_started`, `free_continue_clicked`, trial cancellation
- [ ] Закрепить `two_day_reanchor_contract`: после двух missed check-ins показать last receipt, один friction-вопрос, действие <=5 минут и переписанный if-then trigger — зачем: повторный пропуск чаще означает плохой habit design, а не слабую мотивацию; метрика/риск: `two_day_reanchor_shown`, `reanchor_action_completed`, notification fatigue, D7 retention
- [ ] Добавить copy regression check `bounded_companion_language`: запретить broad/emotional claims вроде "глубоко понимаю тебя" без utility boundary — зачем: свежий risk-based AI regulation signal повышает цену ambiguous companion positioning; метрика/риск: trust complaints, policy/reputation risk, low-trust paywall
- [ ] Протестировать состояние `verified_memory_next_action`: после подтверждения или исправления Day 2-7 receipt следующий шаг явно показывает bridge книга -> вчерашнее действие -> trigger/friction -> today's action <=10 минут — зачем: memory должна менять поведение, а не быть декоративным профилем перед paywall; метрика/риск: `next_action_from_verified_memory`, `too_hard_action_shrink`, wrong-next-action complaints, D7 retention

## 2026-07-08
- [ ] Протестировать proof paywall evidence copy: 3 completed actions + verified receipts + обещание 30-day continuation + free action на сегодня — зачем: paywall должен продавать доказанную цепочку книга -> действие -> привычка, а не абстрактный AI-доступ; метрика/риск: `habit_paywall_proof_rendered`, `trial_started`, `free_continue_clicked`, `paywall_dismissed_no_action`
  - partial 2026-07-08: зафиксирован acceptance contract `proof_paywall_evidence_copy` в `design/2026-07-08.md`; продуктовый Playwright-тест не добавлялся, потому что `/data/workspace/4teacher` уже был dirty в e2e/test-results до cron-run. Next: перенести contract в targeted Playwright spec и только после зеленого теста закрыть пункт.
  - partial 2026-07-08 same-day follow-up: в `/data/workspace/4teacher` добавлен продуктовый Playwright spec `apps/web/e2e/habit-proof-paywall.spec.ts` и обновлён proof paywall copy под acceptance contract: книга/thesis, 3 completed actions, verified/corrected receipts, 30-day continuation, free action <=5 минут, bounded memory copy и events `habit_paywall_proof_rendered`, `trial_started_from_proof`, `free_continue_clicked`. Локальные `tsc`, `lint`, `build` зелёные; targeted Playwright запуск заблокирован окружением (`libnspr4.so` отсутствует), поэтому пункт не закрыт до прогона в среде с browser deps.

## 2026-07-09
- [ ] Добавить acceptance state `two_day_reanchor_needed`: после двух missed check-ins показать last verified receipt, один friction-вопрос, действие <=5 минут и новый if-then trigger — зачем: повторный пропуск чаще означает сломанный habit design, а не нехватку мотивации; метрика/риск: `two_day_reanchor_shown`, `reanchor_action_completed`, notification fatigue, D7 retention
  - partial 2026-07-09: в `/data/workspace/4teacher` добавлен продуктовый state `two_day_reanchor_needed` в habit surface и acceptance spec `apps/web/e2e/habit-two-day-reanchor.spec.ts`: после verified Day 1 receipt можно смоделировать два missed check-ins, увидеть last verified receipt, один friction-вопрос, rewritten if-then trigger и recovery action на 5 минут; добавлены события `two_day_reanchor_shown` и `reanchor_action_completed` (commit 2090744). `lint` и `build` зелёные; targeted Playwright запуск заблокирован окружением (нет Firefox/WebKit browser binaries, Chromium требует `libnspr4.so`), поэтому пункт не закрыт до прогона e2e в среде с browser deps.
- [ ] Ввести copy regression `bounded_companion_language`: запретить broad/emotional claims и заменить их на "помогаю 7 дней внедрять идеи книги в действия; помню только goal/trigger/action/friction" — зачем: state chatbot laws и companion regulation делают ambiguous companion positioning рискованным; метрика/риск: trust complaints, `memory_outside_habit_domain`, low-trust paywall, policy/reputation risk
- [ ] Добавить post-paywall + cost dashboard: `habit_paywall_proof_rendered -> free_continue_clicked -> free_action_completed_after_paywall -> d7_retained -> trial_started -> trial_cancelled`, плюс `llm_calls_per_completed_action` — зачем: AI apps лучше конвертят curiosity, но хуже удерживают подписчиков и имеют variable compute cost; метрика/риск: early-paywall churn, paid curiosity without habit, gross-margin blind spot
- [ ] Перенести design contract `two_day_reanchor_needed` в acceptance spec: два missed check-ins -> last verified receipt -> friction -> rewritten trigger -> действие <=5 минут -> `reanchor_action_completed` — зачем: recovery должен чинить цепочку книга -> действие -> привычка до paywall proof, а не открывать generic chat; метрика/риск: `two_day_reanchor_shown`, `reanchor_trigger_rewritten`, `reanchor_action_completed`, D7 retention

## 2026-07-10
- [x] Автоматизировать copy regression `bounded_companion_language` для onboarding, memory receipt, recovery и proof paywall: запретить broad/emotional claims вроде "понимаю тебя глубоко", "твой AI-друг", "всегда рядом", и разрешить только utility copy "7 дней внедряю идеи книги в действия; помню goal/trigger/action/friction" — зачем: свежий risk-based AI regulation signal усиливает цену ambiguous companion positioning; метрика/риск: trust complaints, `memory_outside_habit_domain`, low-trust paywall, policy/reputation risk *(done 2026-07-10: добавлен source-level regression `test:bounded-companion-language`, который сканирует habit/onboarding/paywall/recovery surfaces на broad companion claims и требует bounded utility copy; commit 2bbb802)*
- [ ] Добавить post-paywall + cost dashboard slice: `habit_paywall_proof_rendered -> free_continue_clicked -> free_action_completed_after_paywall -> d7_retained -> trial_started -> trial_cancelled`, плюс `llm_calls_per_completed_action` и `llm_cost_per_completed_action` — зачем: AI apps могут хорошо конвертить curiosity, но слабее удерживать и дороже обслуживать daily loop; метрика/риск: early-paywall churn, paid curiosity without habit, gross-margin blind spot
- [ ] Протестировать template-first Day 2-7 action guardrail после verified receipt: если есть `goal/trigger/completed_action/friction`, следующий микро-шаг <=10 минут собирается структурно без open-ended chat, а LLM используется только для shrink/clarify fallback — зачем: verified memory должна снижать cost и повышать предсказуемость, не превращаясь в generic answer machine; метрика/риск: `llm_calls_per_completed_action`, action quality score, wrong-next-action complaints, D7 retention
- [ ] Перенести design contract `verified_memory_next_action_template` в acceptance spec: verified/corrected receipt -> template-first action <=10 минут -> fallback только для shrink/clarify/rewrite -> receipt progress после `Сделал` — зачем: следующий день должен продолжать цепочку книга -> действие -> привычка без generic chat и без лишнего LLM-cost; метрика/риск: `template_action_completed`, `template_fallback_used`, `llm_cost_per_completed_action`, wrong-next-action complaints

## 2026-07-11
- [x] Перенести design contract `post_paywall_habit_health` в dashboard spec: показать цепочку `habit_paywall_proof_rendered -> free_continue_clicked -> free_action_completed_after_paywall -> d7_retained -> trial_started -> trial_cancelled` и cost-пару `llm_calls_per_completed_action` / `llm_cost_per_completed_action` — зачем: paywall должен сохранять цепочку книга -> действие -> привычка и не скрывать рост LLM-cost за trial starts; метрика/риск: early-paywall churn, paid curiosity without habit, gross-margin blind spot *(done 2026-07-11: добавлен typed contract `PostPaywallHabitHealthDashboard` и compile-time dashboard spec в `@siamentor/types`, фиксирующий post-paywall funnel, D7 retention, trial cancellation и LLM cost-per-action поля; product commit d056612)*

## 2026-07-12
- [ ] Перенести design contract `offline_verified_memory_action` в acceptance spec: при offline/LLM timeout и verified memory показать локальный action <=5 минут, pending-sync receipt и progress без generic error — зачем: daily habit loop должен продолжаться из книги и verified receipt даже без сети, не увеличивая LLM-cost; метрика/риск: `offline_habit_action_shown`, `offline_action_completed_pending_sync`, `offline_completion_synced`, `llm_calls_per_completed_action`, D7 retention
  - partial 2026-07-12: добавлен продуктовый state `offline_verified_memory_action` в habit surface и acceptance spec `apps/web/e2e/habit-offline-verified-memory-action.spec.ts`: после verified receipt пользователь может открыть offline fallback, увидеть локальный action на 5 минут, pending-sync receipt, progress 2/3 без generic error и события `offline_habit_action_shown` / `offline_action_completed_pending_sync` с `llm_calls_used=0`. `tsc --noEmit`, bounded companion regression и `git diff --check` зелёные; targeted Playwright запуск заблокирован окружением (`libnspr4.so` отсутствует), поэтому пункт не закрыт до прогона e2e в среде с browser deps. Product commit b9e53c0.

## 2026-07-13
- [ ] Перенести design contract `verified_memory_next_action_template` в acceptance spec: verified/corrected receipt -> template-first action <=10 минут -> LLM fallback только для shrink/clarify/rewrite -> completion receipt и progress — зачем: Day 2-7 должен продолжать цепочку книга -> действие -> привычка без generic chat и без лишнего LLM-cost; метрика/риск: `template_action_completed`, `template_fallback_used`, `llm_calls_per_completed_action`, wrong-next-action complaints, D7 retention
  - partial 2026-07-13: добавлен продуктовый acceptance spec `apps/web/e2e/habit-verified-memory-template-action.spec.ts` и Day 2 UI/analytics contract: template-first из verified memory, action <=10 минут, LLM fallback только `shrink/clarify/rewrite`, `llm_calls_used=0` (product commit 8e5dcdb). `lint` с `NEXT_PUBLIC_API_URL=https://api.example.com` и `git diff --check` зелёные; targeted Playwright запуск заблокирован окружением (Firefox/WebKit browser binaries отсутствуют, Chromium требует `libnspr4.so`), поэтому пункт не закрыт до прогона e2e в среде с browser deps.

## 2026-07-14
- [ ] Перенести design contract `offline_action_synced_review` в acceptance spec: offline action completed -> receipt sync -> verified/corrected memory review -> template-first next action <=10 минут -> proof progress учитывает только verified receipt — зачем: offline выполнение должно становиться доказуемой частью цепочки книга -> действие -> привычка, а не теряться перед paywall; метрика/риск: `offline_completion_synced`, `offline_receipt_synced_verified`, `synced_receipt_next_action`, `llm_calls_per_completed_action`, proof eligibility trust
  - partial 2026-07-14: добавлен product state `offline_action_synced_review` и acceptance spec `apps/web/e2e/habit-offline-action-synced-review.spec.ts`: offline action -> sync review -> disabled next action until `Верно` -> events `offline_completion_synced`, `offline_receipt_synced_verified`, `synced_receipt_next_action`, proof counts verified receipts only (product commit 3c02937). `@siamentor/web` lint, `@siamentor/types` tsc и targeted `git diff --check` зелёные; targeted Playwright заблокирован окружением (Chromium executable отсутствует в `/home/openclaw/.cache/ms-playwright/...`), поэтому пункт не закрыт до прогона e2e в среде с browser deps.

## 2026-07-15
- [ ] Перенести design contract `synced_receipt_proof_readiness` в acceptance spec: после offline sync-review verified/corrected receipt засчитывается в proof progress, показывает `2/3 проверенных действий`, открывает template-first next action <=10 минут, но proof paywall появляется только на `3/3` и сохраняет free action — зачем: offline выполнение должно становиться видимым доказательством цепочки книга -> действие -> привычка до монетизации; метрика/риск: `proof_progress_after_sync_review_shown`, `offline_receipt_synced_verified`, `proof_paywall_ready_after_verified_receipts`, `free_continue_clicked`, premature paywall churn
  - partial 2026-07-15: добавлен acceptance spec `apps/web/e2e/habit-synced-receipt-proof-readiness.spec.ts` и UI proof-readiness line в sync-review card: verified offline receipt показывает `2/3 проверенных действий`, не открывает proof paywall до `3/3`, открывает template-first next action <=10 минут, а `/ru/habits/proof` сохраняет free action. Product commit `abb93d5` pushed. `@siamentor/web` lint и build с `NEXT_PUBLIC_API_URL=https://api.example.com` зелёные; targeted Playwright заблокирован окружением: отсутствует `/home/openclaw/.cache/ms-playwright/chromium_headless_shell-1200/chrome-headless-shell-linux64/chrome-headless-shell`, поэтому пункт не закрыт до прогона e2e в среде с browser deps.

## 2026-07-16
- [ ] Перенести design contract `proof_ready_next_action` в acceptance spec: после `3/3` verified receipts показать evidence receipt, paid CTA `Продолжить 30 дней` и free action <=5 минут на том же экране — зачем: paywall должен продавать доказанную цепочку книга -> действие -> привычка, не обрывая сегодняшний habit loop; метрика/риск: `habit_paywall_proof_rendered`, `trial_started_from_proof`, `proof_paywall_dismissed_with_free_action`, `free_action_completed_after_proof`, `paywall_dismissed_no_action`
  - partial 2026-07-16: добавлен acceptance spec `apps/web/e2e/habit-proof-ready-next-action.spec.ts` и стабильные UI-якоря на proof paywall: evidence receipt, paid CTA `Продолжить 30 дней`, цена 999 ₽ и free action `Бесплатное действие на сегодня` на том же экране. Product commit `d6ed6f1` pushed. `@siamentor/web` lint и build с `NEXT_PUBLIC_API_URL=https://api.example.com` зелёные; targeted Playwright заблокирован окружением: отсутствует `/home/openclaw/.cache/ms-playwright/chromium_headless_shell-1200/chrome-headless-shell-linux64/chrome-headless-shell`, поэтому пункт не закрыт до прогона e2e в среде с browser deps.

## 2026-07-17
- [ ] Перенести design contract `corrected_receipt_proof_gate` в acceptance spec: correction review -> saved corrected receipt -> next action from corrected trigger/friction -> proof progress counts only explicitly saved corrected receipt — зачем: исправленная память должна быть видимым доказательством контроля в цепочке книга -> действие -> привычка перед paywall, а не скрытой правкой профиля; метрика/риск: `receipt_correction_saved`, `next_action_from_corrected_receipt`, `proof_progress_corrected_receipt_counted`, proof eligibility trust
  - partial 2026-07-17: добавлен acceptance spec `apps/web/e2e/habit-corrected-receipt-proof-gate.spec.ts` и product gate для явно сохраненного corrected receipt: `receipt_correction_saved`, `next_action_from_corrected_receipt`, corrected receipt считается proof-eligible только при `verified_at`, следующий template-first action строится из исправленного trigger/friction. Product commit `a8e83b5` pushed. `@siamentor/types` tsc, `@siamentor/web` lint, production build и `git diff --check` зелёные; targeted Playwright заблокирован окружением: отсутствует `/home/openclaw/.cache/ms-playwright/chromium_headless_shell-1200/chrome-headless-shell-linux64/chrome-headless-shell`, поэтому пункт не закрыт до прогона e2e в среде с browser deps.

## 2026-07-18
- [ ] Перенести design contract `forgotten_receipt_proof_boundary` в acceptance spec: `Забыть` receipt -> receipt не используется в next action -> proof progress пересчитан -> paywall блокируется до нового verified/corrected action — зачем: privacy-control должен реально менять цепочку книга -> действие -> привычка перед paywall, а не быть декоративной кнопкой; метрика/риск: `habit_memory_deleted_by_user`, `proof_progress_after_forget`, `next_action_excludes_forgotten_receipt`, proof eligibility trust
  - partial 2026-07-18: добавлен acceptance spec `apps/web/e2e/habit-forgotten-receipt-proof-boundary.spec.ts` и product analytics/UI boundary: кнопка `Забыть` очищает completed action/trigger/friction, next action показывает новый шаг без старой памяти, Day 2/offline actions остаются disabled, proof eligibility явно пишет `forgotten receipt excluded`, событие `habit_memory_deleted_by_user` несет `proof_progress_after_forget=0` и `next_action_excludes_forgotten_receipt=true`. Product commit `a9210ef` pushed. `@siamentor/web` lint, production build с `NEXT_PUBLIC_API_URL=https://api.example.com` и `git diff --check` зелёные; targeted Playwright заблокирован окружением: отсутствует `/home/openclaw/.cache/ms-playwright/chromium_headless_shell-1200/chrome-headless-shell-linux64/chrome-headless-shell`, поэтому пункт не закрыт до прогона e2e в среде с browser deps.

## 2026-07-19
- [ ] Перенести design contract `forgotten_receipt_rebuild` в acceptance spec: после `Забыть` показать reset proof `0/3`, сохранить book/thesis anchor, создать новое действие <=5 минут без deleted receipt и заблокировать stale proof CTA — зачем: privacy-control должен восстанавливать цепочку книга -> действие -> привычка без давления вернуть память ради paywall; метрика/риск: `habit_memory_deleted_by_user`, `forgotten_receipt_rebuild_shown`, `new_receipt_after_forget_created`, `stale_proof_cta_blocked`, proof eligibility trust
  - partial 2026-07-19: добавлен vertical slice product flow `forgotten_receipt_rebuild`: после `Забыть` UI показывает reset proof `0/3`, сохраняет book/thesis anchor, блокирует stale proof CTA, строит новое действие на 5 минут без deleted receipt и создает новый pending receipt; добавлен acceptance spec `apps/web/e2e/habit-forgotten-receipt-rebuild.spec.ts` с analytics contract `forgotten_receipt_rebuild_shown` / `new_receipt_after_forget_created`. Product commit 89411eb pushed. `@siamentor/types` tsc, `@siamentor/web` lint, production build и `git diff --check` зелёные; targeted Playwright заблокирован окружением: отсутствует `/home/openclaw/.cache/ms-playwright/chromium_headless_shell-1200/chrome-headless-shell-linux64/chrome-headless-shell`, поэтому пункт не закрыт до прогона e2e в среде с browser deps.

## 2026-07-20
- [ ] Перенести design contract `stale_proof_recovery_after_forget` в acceptance spec: stale proof entrypoint после `Забыть` должен скрыть paid CTA, показать reset `0/3`, сохранить book/thesis anchor и предложить новое действие <=5 минут без deleted receipt — зачем: paywall не должен продавать устаревшее доказательство после privacy-control; метрика/риск: `stale_proof_entrypoint_blocked`, `forgotten_receipt_rebuild_shown`, `new_receipt_after_forget_created`, `trial_started_from_stale_proof`, proof eligibility trust
  - partial 2026-07-20: уточнен wiki acceptance contract в `design/2026-07-20.md`: сценарий stale proof entrypoint после `Забыть`, обязательные assertions, events `stale_proof_entrypoint_blocked` / `forgotten_receipt_rebuild_shown` / `new_receipt_after_forget_created`, запрет `trial_started_from_stale_proof`, и implementation notes про reuse `forgotten_receipt_rebuild`. Product repo не тронут, потому что `/data/workspace/4teacher` уже был dirty в e2e/test-results и `apps/web/src/components/features/habit-proof-dashboard.tsx`; next: перенести contract в `apps/web/e2e/habit-stale-proof-recovery-after-forget.spec.ts` и product guard, затем закрыть пункт после проверок.

## 2026-07-21
- [ ] Перенести design contract `rebuild_proof_after_forget_action` в acceptance spec: после нового действия из forgotten-rebuild показать recovery receipt review, progress `1/3 новых проверенных действий`, next action только из нового verified/corrected receipt и скрытый paywall до `3/3` — зачем: privacy-control должен восстанавливать доказанную цепочку книга -> действие -> привычка без возврата удаленной памяти; метрика/риск: `forgotten_receipt_rebuild_action_completed`, `new_receipt_verified_after_forget`, `new_receipt_corrected_after_forget`, `paywall_still_blocked_after_first_rebuild`, proof eligibility trust
  - partial 2026-07-21: уточнен wiki acceptance contract в `design/2026-07-21.md`: scenario для `habit-rebuild-proof-after-forget-action.spec.ts`, required assertions по `1/3 новых проверенных действий`, запрет paid-start events на `1/3`, события `forgotten_receipt_rebuild_action_completed` / `new_receipt_verified_after_forget` / `new_receipt_corrected_after_forget` / `paywall_still_blocked_after_first_rebuild`, и implementation notes про template-first recovery без deleted receipt text. Product repo не тронут, потому что `/data/workspace/4teacher` уже был dirty в e2e/test-results и `apps/web/src/components/features/habit-proof-dashboard.tsx`; next: перенести contract в `apps/web/e2e/habit-rebuild-proof-after-forget-action.spec.ts` и product state после очистки/разбора dirty changes.

## 2026-07-22
- [ ] Перенести design contract `second_rebuild_receipt_after_forget` в acceptance spec: после privacy reset и первого нового действия показать receipt review, progress `1/3 новых проверенных действий`, второе действие только из нового verified/corrected receipt и скрытый paywall до `3/3` — зачем: privacy-control должен заново строить цепочку книга -> действие -> привычка без смешивания удаленной памяти; метрика/риск: `new_proof_progress_after_forget_1_of_3`, `second_rebuild_action_ready`, `deleted_receipt_excluded_from_rebuild`, premature paywall after reset
  - partial 2026-07-22: уточнен wiki acceptance contract в `design/2026-07-22.md`: spec `habit-second-rebuild-receipt-after-forget.spec.ts`, scenario после privacy reset и первого нового verified/corrected receipt, required assertions по `1/3 новых проверенных действий`, `second_rebuild_action_ready`, исключению deleted receipt из DOM/event/action source/proof eligibility и запрету paid-start events до `3/3`. Product repo не тронут, потому что `/data/workspace/4teacher` уже был dirty в e2e/test-results и `apps/web/src/components/features/habit-proof-dashboard.tsx`; next: перенести contract в product acceptance spec и UI state после очистки/разбора dirty changes. Commit 92fdb4f.

## 2026-07-23
- [ ] Перенести design contract `third_rebuild_action_gate_after_forget` в acceptance spec: после privacy reset и двух новых verified/corrected действий показать progress `2/3 новых проверенных действий`, третье действие только из новой chain, deleted receipt excluded, paid CTA скрыт до review третьего receipt — зачем: paywall должен открываться только на доказанной новой цепочке книга -> действие -> привычка после privacy-control; метрика/риск: `new_proof_progress_after_forget_2_of_3`, `third_rebuild_action_ready`, `deleted_receipt_excluded_from_third_rebuild`, premature paywall after reset
  - partial 2026-07-23: уточнен wiki acceptance contract в `design/2026-07-23.md`: spec `habit-third-rebuild-action-gate-after-forget.spec.ts`, required assertions по `2/3 новых проверенных действий после сброса`, третьему действию <=5 минут, запрету paid-start events до review третьего receipt, negative fixture для deleted receipt и implementation notes по sanitized context/proof eligibility. Product repo не тронут, потому что `/data/workspace/4teacher` уже был dirty в e2e/test-results и `apps/web/src/components/features/habit-proof-dashboard.tsx`; next: перенести contract в product acceptance spec и UI state после очистки/разбора dirty changes.

## 2026-07-24
- [ ] Перенести design contract `final_rebuild_receipt_before_paywall` в acceptance spec: после privacy reset и третьего нового completed action сначала показать pending receipt review с progress `2/3`, скрытым paid CTA и excluded deleted receipt; paywall proof `3/3 новых проверенных действий после сброса` разрешать только после `Верно` или сохраненной коррекции — зачем: paywall должен монетизировать проверенную новую цепочку книга -> действие -> привычка, а не сырой completion после privacy-control; метрика/риск: `third_rebuild_action_completed_pending_receipt`, `new_proof_ready_after_forget_3_of_3`, `third_rebuild_receipt_forgotten`, `trial_started_from_proof`, proof eligibility trust
  - partial 2026-07-24: усилен wiki acceptance contract в `design/2026-07-24.md`: scenario для `habit-final-rebuild-receipt-before-paywall.spec.ts`, события `third_rebuild_action_completed_pending_receipt` / `paywall_blocked_until_final_rebuild_receipt_review` / `new_proof_ready_after_forget_3_of_3`, negative fixture с deleted receipt и implementation notes по pending receipt gate. Product repo не тронут, потому что `/data/workspace/4teacher` уже был dirty в e2e/test-results и `apps/web/src/components/features/habit-proof-dashboard.tsx`; next: перенести contract в product acceptance spec и UI/event guard после очистки/разбора dirty changes.

## 2026-07-25
- [ ] Перенести design contract `paywall_after_final_rebuild_receipt` в acceptance spec: после privacy reset и review третьего нового receipt показать proof paywall `3/3 новых проверенных действия после сброса`, book/thesis anchor, paid CTA, free continue action и исключить deleted receipt из DOM/event/proof/action source — зачем: paywall должен продавать продолжение новой доказанной цепочки книга -> действие -> привычка, а не generic AI-доступ или старую память; метрика/риск: `paywall_after_final_rebuild_receipt_shown`, `trial_started_from_rebuilt_proof`, `free_continue_after_rebuild_clicked`, proof eligibility trust
  - partial 2026-07-25: добавлен wiki acceptance contract в `design/2026-07-25.md` и продуктовый acceptance spec `apps/web/e2e/habit-paywall-after-final-rebuild-receipt.spec.ts`: проверяет rebuilt proof `3/3 новых проверенных действия после сброса`, book/thesis anchor, paid CTA, free continue action, события `paywall_after_final_rebuild_receipt_shown` / `trial_started_from_rebuilt_proof` / `free_continue_after_rebuild_clicked` и отсутствие deleted receipt в DOM/event payload/action source. Пункт не закрыт полностью, потому что `/data/workspace/4teacher` уже был dirty до cron-run, а spec требует product route/state `/ru/habits/rebuild-proof`; next: добавить UI/event guard в `habit-proof-dashboard` после разбора dirty changes и прогнать targeted Playwright в среде с browser deps.

## 2026-07-26
- [ ] Перенести design contract `free_continue_after_rebuilt_paywall` в acceptance spec: после rebuilt proof paywall и CTA `Продолжить бесплатно сегодня` показать действие <=5 минут только из новой verified chain, receipt review без paid CTA до проверки и исключить deleted receipt из DOM/event/action source — зачем: отказ от оплаты должен продолжать цепочку книга -> действие -> привычка, а не вести в generic chat или stale memory; метрика/риск: `free_continue_after_rebuild_clicked`, `free_rebuild_action_completed`, `free_rebuild_receipt_verified`, `deleted_receipt_excluded_from_free_continue`, paywall trust
  - partial 2026-07-26: добавлен продуктовый acceptance spec `apps/web/e2e/habit-free-continue-after-rebuilt-paywall.spec.ts`: проверяет CTA `Продолжить бесплатно сегодня`, free action <=5 минут из `rebuilt_verified_chain`, receipt review без paid CTA до проверки, события `free_continue_after_rebuild_clicked` / `free_rebuild_action_completed` / `free_rebuild_receipt_verified` и отсутствие deleted receipt в DOM/event payload/action source. Пункт не закрыт полностью, потому что `/data/workspace/4teacher` уже был dirty до cron-run, а route/state `/ru/habits/rebuild-proof` ещё требует UI/event implementation; next: реализовать `free-continue-after-rebuilt-paywall` и `free-rebuild-receipt-review` в product flow, затем прогнать targeted Playwright.

## 2026-07-27
- [ ] Перенести design contract `free_rebuild_receipt_review` в acceptance spec: после free continue на rebuilt paywall и выполненного действия показать pending receipt review без paid CTA, progress `3/3 проверенных действия + сегодняшнее ждет проверки`, verified/corrected сохранение в новую chain и исключение deleted receipt из DOM/event/action source — зачем: бесплатное продолжение после paywall должно укреплять цепочку книга -> действие -> привычка, а не засчитывать сырой completion или давить оплатой; метрика/риск: `free_rebuild_action_completed`, `free_rebuild_receipt_verified`, `free_rebuild_receipt_corrected`, `deleted_receipt_excluded_from_free_review`, paywall trust
  - partial 2026-07-27: добавлен wiki acceptance contract в `design/2026-07-27.md` и продуктовый acceptance spec `apps/web/e2e/habit-free-rebuild-receipt-review.spec.ts`: проверяет pending receipt review после free continue, progress `3/3 проверенных действия + сегодняшнее ждет проверки`, скрытый paid CTA до review, verified/corrected сохранение в `rebuilt_verified_chain`, события `free_rebuild_action_completed` / `free_rebuild_receipt_verified` / `free_rebuild_receipt_corrected` и отсутствие deleted receipt в DOM/event payload/action source. Пункт не закрыт полностью, потому что `/data/workspace/4teacher` уже был dirty до cron-run, а route/state `/ru/habits/rebuild-proof` ещё требует UI/event implementation; next: реализовать `free-rebuild-receipt-review` в product flow и прогнать targeted Playwright.

## 2026-07-28
- [ ] Перенести design contract `free_rebuild_receipt_verified_loop` в acceptance spec: после `Верно` или сохраненной коррекции free receipt показать success state `Проверено. Цепочка продолжается`, progress новой chain, next action <=5 минут только из verified/corrected free receipt и скрыть paid CTA до session boundary — зачем: бесплатное продолжение после rebuilt paywall должно укреплять цепочку книга -> действие -> привычка, а не сразу возвращать пользователя в оплату или generic chat; метрика/риск: `free_rebuild_receipt_verified`, `free_rebuild_receipt_corrected`, `free_rebuild_next_action_started`, `rebuilt_paywall_suppressed_after_free_review`, paywall trust
  - partial 2026-07-28: усилен wiki acceptance contract в `design/2026-07-28.md`: spec `habit-free-rebuild-receipt-verified-loop.spec.ts`, headline `Проверено. Цепочка продолжается`, progress `4 проверенных действия после сброса`, next action <=5 минут только из verified/corrected free receipt, скрытый paid CTA до session boundary, события `free_rebuild_next_action_started` / `rebuilt_paywall_suppressed_after_free_review` и отсутствие deleted receipt в DOM/event/action/proof source. Пункт не закрыт полностью, потому что `/data/workspace/4teacher` уже был dirty до cron-run; next: перенести contract в product acceptance spec и реализовать loop state в `/ru/habits/rebuild-proof`.

## 2026-07-29
- [ ] Перенести design contract `free_rebuild_session_boundary` в acceptance spec: после verified/corrected free receipt и новой сессии показать progress `4 проверенных действия после сброса`, next action <=5 минут из `rebuilt_verified_chain`, paid CTA только ниже действия, free completion -> receipt review, deleted receipt excluded — зачем: paywall после бесплатного продолжения должен поддерживать доказанную цепочку книга -> действие -> привычка, а не ломать trust немедленной блокировкой; метрика/риск: `free_rebuild_session_boundary_ready`, `session_boundary_next_action_started`, `session_boundary_paid_cta_seen`, `session_boundary_free_action_completed`, paywall trust
  - partial 2026-07-29: добавлен продуктовый acceptance spec `apps/web/e2e/habit-free-rebuild-session-boundary.spec.ts`: проверяет новую сессию после verified/corrected free receipt, progress `4 проверенных действия после сброса`, next action <=5 минут из `rebuilt_verified_chain`, paid CTA ниже действия, free completion -> pending receipt review, события `free_rebuild_session_boundary_ready` / `session_boundary_next_action_started` / `session_boundary_paid_cta_seen` / `session_boundary_free_action_completed` и отсутствие deleted receipt в DOM/event/action source. Пункт не закрыт полностью, потому что `/data/workspace/4teacher` уже был dirty до cron-run, а `/ru/habits/rebuild-proof?state=session-boundary` ещё требует UI/event implementation; next: реализовать session-boundary state в product flow и прогнать targeted Playwright.

## 2026-07-30
- [ ] Перенести design contract `session_boundary_paid_cta_below_action` в acceptance spec: новая сессия после verified/corrected free receipt показывает action <=5 минут из `rebuilt_verified_chain`, paid CTA ниже completion, CTA скрыт во время pending receipt review и deleted receipt excluded — зачем: paywall должен монетизировать доказанную привычку, не ломая цепочку книга -> действие -> привычка; метрика/риск: `session_boundary_paid_cta_seen`, `session_boundary_paid_cta_dismissed`, `session_boundary_free_action_completed`, `free_rebuild_receipt_verified`, paywall trust
  - partial 2026-07-30: усилен wiki acceptance contract в `design/2026-07-30.md`: spec `habit-session-boundary-paid-cta-below-action.spec.ts`, state `session_boundary_paid_cta_below_action`, progress `4 проверенных действия после сброса`, action <=5 минут из `rebuilt_verified_chain`, paid CTA ниже `Сделал`, dismiss event `session_boundary_paid_cta_dismissed`, pending receipt review без paid CTA и исключение deleted receipt из DOM/event/action source. Пункт не закрыт полностью, потому что `/data/workspace/4teacher` уже был dirty до cron-run; next: перенести contract в product acceptance spec, реализовать UI/event state в `/ru/habits/rebuild-proof?state=session-boundary-paid-cta-below-action` и прогнать targeted Playwright.

## 2026-07-31
- [ ] Перенести design contract `session_boundary_receipt_gate` в acceptance spec: после free completion в новой сессии показать pending receipt review с progress `4 проверенных действия после сброса + сегодняшнее ждет проверки`, скрытым paid CTA до проверки и исключением deleted receipt — зачем: paywall должен возвращаться только после проверенного результата, сохраняя цепочку книга -> действие -> привычка; метрика/риск: `session_boundary_receipt_gate_shown`, `session_boundary_receipt_verified`, `session_boundary_receipt_corrected`, `session_boundary_receipt_forgotten`, premature paywall after free completion
  - partial 2026-07-31: добавлен продуктовый acceptance spec `apps/web/e2e/habit-session-boundary-receipt-gate.spec.ts`: проверяет pending receipt review `4 проверенных действия после сброса + сегодняшнее ждет проверки`, скрытый paid CTA/price до review, события `session_boundary_receipt_gate_shown` / `session_boundary_receipt_verified` / `session_boundary_receipt_corrected` / `session_boundary_receipt_forgotten`, сохранение verified/corrected receipt в `rebuilt_verified_chain`, forget без premature paywall и отсутствие deleted receipt в DOM/event payload. Пункт не закрыт полностью, потому что `/data/workspace/4teacher` уже был dirty до cron-run, а `/ru/habits/rebuild-proof?state=session-boundary-receipt-gate` ещё требует UI/event implementation; next: реализовать state и прогнать targeted Playwright.

## 2026-08-01
- [ ] Перенести design contract `session_boundary_paywall_return` в acceptance spec: после verified/corrected receipt в новой сессии показать success `Проверено. Цепочка продолжается`, progress `5 проверенных действий после сброса`, next action <=5 минут из `rebuilt_verified_chain` и paid CTA только ниже действия — зачем: paywall должен возвращаться после доказанного результата, не ломая цепочку книга -> действие -> привычка; метрика/риск: `session_boundary_paywall_return_shown`, `session_boundary_paid_cta_clicked_after_review`, `session_boundary_paid_cta_dismissed_after_review`, `session_boundary_next_action_completed_after_return`, paywall trust
  - partial 2026-08-01: добавлен продуктовый acceptance spec `apps/web/e2e/habit-session-boundary-paywall-return.spec.ts`: проверяет success `Проверено. Цепочка продолжается`, progress `5 проверенных действий после сброса`, next action <=5 минут из `rebuilt_verified_chain`, paid CTA/price ниже действия, события `session_boundary_paywall_return_shown` / `session_boundary_paid_cta_clicked_after_review` / `session_boundary_paid_cta_dismissed_after_review` / `session_boundary_next_action_completed_after_return` и отсутствие deleted receipt в DOM/event/action/proof source. Пункт не закрыт полностью, потому что `/data/workspace/4teacher` уже был dirty до cron-run, route/state `/ru/habits/rebuild-proof?state=session-boundary-paywall-return` ещё требует UI/event implementation, а targeted Playwright заблокирован browser dependency `libnspr4.so`; next: реализовать state, восстановить Playwright deps и прогнать targeted spec.

## 2026-08-02
- [ ] Перенести design contract `session_boundary_paid_decline_loop` в acceptance spec: после dismiss paid CTA показать один бесплатный action <=5 минут из `rebuilt_verified_chain`, скрыть paid CTA до receipt review, progress держать как `5 проверенных действий после сброса + сегодняшнее ждет проверки`, после verified/corrected receipt вернуть next action и paid CTA ниже действия — зачем: отказ от оплаты не должен ломать доказанную цепочку книга -> действие -> привычка; метрика/риск: `session_boundary_paid_cta_dismissed_after_review`, `paid_decline_free_action_completed`, `paid_decline_receipt_verified`, premature paywall after dismiss, paywall trust
  - partial 2026-08-02: добавлен продуктовый acceptance spec `apps/web/e2e/habit-session-boundary-paid-decline-loop.spec.ts`: проверяет dismiss paid CTA, бесплатный action <=5 минут из `rebuilt_verified_chain`, скрытый paid CTA до receipt review, progress `5 проверенных действий после сброса + сегодняшнее ждет проверки`, события `session_boundary_paid_cta_dismissed_after_review` / `paid_decline_free_action_completed` / `paid_decline_receipt_verified`, возврат next action и paid CTA ниже действия после verify, а также отсутствие deleted receipt в DOM/event/action/proof source. Пункт не закрыт полностью, потому что `/data/workspace/4teacher` уже был dirty до cron-run, route/state `/ru/habits/rebuild-proof?state=session-boundary-paid-decline-loop` ещё требует UI/event implementation; next: реализовать state и прогнать targeted Playwright.
